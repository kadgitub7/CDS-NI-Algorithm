# Comparative Algorithmic Analysis: CDS-OVR vs. Benchmark Methods on the UCI Arrhythmia Dataset

## 1. Introduction and Scope

This document provides a rigorous, mechanism-level analysis of the CDS-OVR (Class-Directed Splitting with One-vs-Rest decomposition) classifier against six benchmark studies and the original CDS algorithm on the UCI Arrhythmia dataset. Rather than merely reporting accuracy differences, this analysis traces each performance gap to specific algorithmic design decisions, dataset properties, and their interactions. Every claim is grounded in either (a) the published methodology and results of the benchmark paper, (b) the CDS-OVR source code (`cds_ovr.py`, W11-01 configuration), or (c) verifiable properties of the UCI Arrhythmia dataset.

### 1.1 Consolidated Results

| Method | Protocol | Accuracy | Task | N_test | Seeds / Variance |
|--------|----------|----------|------|--------|-----------------|
| **CDS-OVR (W11-01)** | 90/10 split | **92.9% best / 86.2% mean** | 8-class multiclass | 42 | 10 seeds, σ=4.97% |
| **CDS-OVR (W11-01)** | 90/10 split | **97.6% best / 88.6% mean** | Binary | 42 | 10 seeds, σ=4.74% |
| **CDS-OVR (W11-01)** | 10-fold CV | **86.8% best / 84.7% mean** | 8-class multiclass | 416 (all) | 10 seeds, σ=0.93% |
| **CDS-OVR (W11-01)** | 60/40 split | **86.2% best / 80.2% mean** | 8-class multiclass | 167 | 10 seeds, σ=3.06% |
| Sharma 2024 (Optimized RF) | 90/10 split | 95.24% | Binary | 42 | Single split, no variance |
| Mustaqeem 2018 (SVM-OAO) | 90/10 split | 92.07% | 16-class multiclass | ~45 | Single split, no variance |
| Plawiak 2018 (Evolutionary NN) | 10-fold CV | 86.57% | 17-class multiclass | 452 (all) | Not reported |
| Irfan 2022 (CNN-LSTM) | 60/40 split | 93.33% | 13-class multiclass | 181 | Single split, no variance |
| Hybrid FS 2021 (Ensemble) | Not specified | 77.27% | Binary | Not specified | Not reported |
| Jadhav 2012 (MLP) | 85/15 split | 86.67% | Binary | ~68 | Single split |
| Islam 2023 (SVM+RF) | Not specified | 77.4% | Multiclass | Not specified | Not reported |

#### CDS-OVR Complete Results Across All Split Ratios (10 seeds each)

*Generated by `run_all_splits.py` — seeds: [13, 20, 27, 34, 41, 48, 55, 62, 69, 76]*

| Split | N_test | Multi Best | Multi Mean | Multi σ | Binary Best | Binary Mean | Binary σ |
|-------|--------|-----------|-----------|---------|------------|------------|---------|
| 50/50 | 208 | 85.1% | 79.4% | 2.85% | 87.5% | 83.0% | 2.65% |
| 60/40 | 167 | 86.2% | 80.2% | 3.06% | 92.2% | 84.5% | 3.39% |
| 70/30 | 125 | 87.2% | 81.4% | 3.75% | 90.4% | 85.1% | 4.04% |
| 75/25 | 104 | 86.5% | 82.5% | 3.21% | 91.3% | 85.8% | 3.64% |
| 80/20 | 84 | 89.3% | 85.5% | 2.43% | 94.0% | 88.0% | 3.35% |
| 85/15 | 63 | 90.5% | 85.9% | 2.60% | 96.8% | 88.9% | 3.25% |
| 90/10 | 42 | 92.9% | 86.2% | 4.97% | 97.6% | 88.6% | 4.74% |
| 10-fold CV | 416 | 86.8% | 84.7% | 0.93% | 88.7% | 87.7% | — |

### 1.2 Statistical Context for All Comparisons

A methodological observation underlies every comparison in this document: **CDS-OVR is the only method that reports variance across multiple random seeds.** All six benchmark papers report single-split or single-run results without confidence intervals. On a dataset of 452 patients (or 416 after class removal), small test sets (42 patients at 90/10) produce high variance — CDS-OVR's own 90/10 results range from 76.2% to 92.9% across 10 seeds (a 16.7 pp spread). Any single-split result from any method falls somewhere in an unreported distribution. This does not invalidate the benchmark results, but it means that accuracy differences smaller than approximately ±5 pp on 90/10 splits cannot be confidently attributed to algorithmic superiority without further evidence.

The full split-ratio sweep confirms a monotonic trend: CDS-OVR mean multiclass accuracy rises from 79.4% (50/50) to 86.2% (90/10), a +6.8 pp improvement driven by increased per-class training data enabling better posterior estimates. Variance also increases with smaller test sets: σ = 2.85% at 50/50 (N=208) vs. σ = 4.97% at 90/10 (N=42). The 10-fold CV result (84.7% mean, σ=0.93%) is the most stable estimate because all 416 patients are tested.

This analysis therefore focuses on identifying **structural** reasons for performance differences, mechanisms that would produce consistent advantages or disadvantages across multiple splits rather than attributing small numerical differences to algorithmic design.

---

## 2. Dataset Properties That Determine Classifier Performance

The UCI Arrhythmia dataset has six structural properties that interact with classifier design choices to produce the observed accuracy differences. Understanding these properties is prerequisite to understanding why any classifier achieves its reported numbers.

### 2.1 Class Distribution

After CDS-OVR's removal of classes with insufficient representation (original UCI classes 7, 8, 14, 15, 16 — internally remapped to 7, 8, 11, 12, 13 via `cds_ovr.py` line 61 — totaling 36 patients across 5 classes: class 7 has 3, class 8 has 2, class 14 has 4, class 15 has 5, class 16/Other has 22 patients), the working dataset contains 416 patients across 8 classes:

| Class | Condition | n | % | n_train (90/10) | n_train (60/40) |
|-------|-----------|---|---|----------------|-----------------|
| 1 | Normal | 245 | 58.9% | ~220 | ~147 |
| 2 | Ischemic Changes (CAD) | 44 | 10.6% | ~40 | ~26 |
| 3 | Old Anterior MI | 15 | 3.6% | ~14 | ~9 |
| 4 | Old Inferior MI | 15 | 3.6% | ~14 | ~9 |
| 5 | Sinus Tachycardia | 13 | 3.1% | ~12 | ~8 |
| 6 | Sinus Bradycardia | 25 | 6.0% | ~23 | ~15 |
| 9 | Left Bundle Branch Block | 9 | 2.2% | ~8 | ~5 |
| 10 | Right Bundle Branch Block | 50 | 12.0% | ~45 | ~30 |

The imbalance ratio between the largest class (Normal, 245 patients) and smallest class (LBBB, 9 patients) is 27.2:1. This ratio is the single most important dataset property affecting classifier design. Methods that treat all classes uniformly (identical hyperparameters, shared feature sets, coupled probability outputs) will systematically underperform on the rare disease classes (classes 4, 5, 9 with 15, 13, 9 patients respectively, and class 3 with 15 patients), which collectively contain 52 patients (12.5% of the dataset) but represent 4 of 7 disease categories.

### 2.2 Feature Structure

The 279 features comprise:
- **Demographic features** (4): age, sex, height, weight
- **Continuous ECG measurements** (~206): intervals, durations, amplitudes, axes across 12 leads
- **Binary morphological indicators** (~69): presence/absence of specific waveform features

Three properties of this feature structure directly affect classifier accuracy:

**Inter-feature correlation.**

> **Experiment: Redundancy in Sharma's Feature Set** (`evidence_analysis.py`, Section 3)
>
> **What was tested**: Among the 106 features that pass Sharma's |r| > 0.1 threshold, all pairwise Pearson correlations were computed to identify redundant feature pairs.
>
> **Why**: If features that pass an individual correlation threshold are also highly correlated with each other, retaining both adds model complexity without adding discriminative information. This would mean Sharma's 101-feature set contains substantial redundancy that wastes capacity.
>
> **Method**: For all C(106,2) = 5,565 feature pairs among the retained features, pairwise Pearson |r| was computed (using the 416-patient CDS-OVR dataset, NaN values excluded pairwise). Pairs exceeding |r| > 0.8 (sharing >64% variance) and |r| > 0.9 (sharing >81% variance) were counted.
>
> **Results**:
> - **32 feature pairs exceed |r| > 0.8** (sharing >64% of variance)
> - **2 pairs exceed |r| > 0.9** (sharing >81% of variance)
> - These 32 pairs involve ~40 distinct features, meaning approximately 38% of the retained set participates in at least one highly redundant pair
>
> **Interpretation**: Sharma's feature selection retains features based on their individual correlation with the target, without checking inter-feature redundancy. The 32 highly correlated pairs represent capacity wasted on near-duplicate information. CDS-OVR's CORR_THRESHOLD = 0.8 (per branch) explicitly prevents this: within each sex-branch's 18-feature set, no two features share more than 64% variance, ensuring each slot adds genuinely new discriminative signal.

ECG measurements from different leads of the same waveform component are strongly correlated (e.g., P-wave amplitude across leads). The experiment above demonstrates that any feature selection method treating features independently (ranking each by its individual correlation with the target) will retain such redundant features.

**Concentrated missing values.**

> **Experiment: Missing Value Distribution and Diagnostic Signal** (`evidence_analysis.py`, Sections 2, 12)
>
> **What was tested**: (A) The distribution of missing values across features and patients in the 416-patient CDS-OVR dataset. (B) Whether features with high missing rates carry diagnostic signal (Fisher discriminant ratios for those features).
>
> **Why**: Methods that delete columns or rows with missing values (Sharma, Gupta) assume the deleted data is uninformative. If missing values are concentrated in a few features, and those features carry diagnostic signal for specific diseases, deletion discards useful information and disproportionately removes minority-class patients.
>
> **Method**: (A) For each of 279 features, count NaN values. For each patient, count features with NaN. (B) For each feature with missing values, compute FDR for each class using available (non-NaN) values.
>
> **Results**:
> - Overall missing rate: **0.320% (371/116,064 cells)**
> - Missing values concentrated in 4 features: feature 13 = **83.9% missing** (349/416), feature 11 = 3.4%, feature 10 = 1.7%, feature 12 = 0.2%
> - **354/416 patients (85.1%)** have at least one missing value
> - Feature 13 FDR: **1.437 for class 3** (Old Anterior MI), ranking #19 of 279 features for that class
> - Feature 12 FDR: 0.274 for class 5 (Sinus Tachycardia)
>
> **Interpretation**: Despite 83.9% missing values, feature 13 is the 19th most discriminative feature for class 3 among all 279 candidates. The 67 patients (16.1%) who have valid values for feature 13 provide enough signal to contribute meaningfully to class 3 detection. Deleting this column (as Sharma does) removes diagnostic signal that CDS-OVR exploits. Additionally, the 85.1% patient-level missing rate means row deletion approaches must either discard most patients or accept incomplete data — Sharma's approach removes 32 patients (7.1%), disproportionately from minority classes (Section 5.1.3).

The overall missing rate is 0.320% (371/116,064 cells), but the experiment above shows it is concentrated in 4 features. Methods that delete columns with high missing rates (Gupta et al. 2014: columns with missing values, 279→252; Sharma 2024: column 14 + rows) discard features that carry diagnostic signal — feature 13's FDR of 1.437 for class 3 confirms it is genuinely useful despite being 83.9% missing.

**Sex-dependent distributions.**

> **Experiment: Quantifying Sex Effects on ECG Feature Distributions** (`evidence_analysis.py`, Section 8)
>
> **What was tested**: For all 279 features, the distribution of values was compared between male (n=179) and female (n=237) patients using Cohen's d (standardized mean difference).
>
> **Why**: If features have substantially different distributions between sexes, a classifier trained on the combined population must learn more complex decision boundaries than one trained on sex-stratified subpopulations. Quantifying the number and magnitude of sex-affected features validates whether sex branching is a justified preprocessing step.
>
> **Method**: For each continuous feature, Cohen's d = (μ_male − μ_female) / s_pooled was computed, where s_pooled = √[(s²_male + s²_female)/2]. Features with |d| > 0.5 (medium effect size per Cohen's conventions) were flagged as having clinically meaningful sex-dependent variation.
>
> **Results**:
> - **4 features have Cohen's d > 0.5** (medium-or-larger effect):
>   - Feature 4: d = 0.677 (male mean = 94.25, female mean = 84.26)
>   - Feature 190: d = 0.536
>   - Feature 252: d = 0.520
>   - Feature 3: d = 0.511
> - Class 3 (Old Anterior MI): **100% male (15/15 patients)**
> - Class composition by sex: 5 of 8 classes have male percentage between 35–55% (relatively balanced); class 3 is the extreme outlier
>
> **Interpretation**: The 4 features with d > 0.5 demonstrate that ECG measurements differ measurably between sexes. A classifier pooling both sexes must learn boundaries that accommodate both distributions simultaneously. More critically, class 3's 100% male composition means that sex is a near-perfect discriminator for this class — a sex-stratified model can use this signal directly, while a combined model treats it as one of 279 features with diluted importance. CDS-OVR's sex branching (+0.41 pp overall per ablation) exploits this structure.

Feature 1 (sex) is binary (179 male, 237 female). The experiment above shows 4 features with medium-or-larger sex effects and that class 3 (Old Anterior MI) is 100% male, making sex branching a justified design choice. Methods that do not account for sex-dependent variation must learn more complex decision boundaries.

### 2.3 Interaction Between Dataset Properties and Classifier Design

| Dataset Property | Classifier Design Vulnerability | Affected Benchmarks |
|-----------------|-------------------------------|-------------------|
| 61:1 class imbalance | Uniform hyperparameters suppress rare classes | All six benchmarks |
| 279 features, 416 patients | Curse of dimensionality degrades distance-based methods | Islam (SVM/RF), Jadhav (MLP without FS) |
| Correlated features | Redundant feature selection wastes model capacity | Mustaqeem (94 features), Sharma (101 features) |
| Concentrated missing values | Column deletion removes discriminative features | Islam (279→252), Sharma (dropped column 14 + rows) |
| Sex-dependent distributions | Combined population requires more complex boundaries | All benchmarks except CDS-OVR |
| Small disease classes | Pairwise classifiers / SoftMax coupling disadvantage rare classes | Mustaqeem (OAO), Irfan (SoftMax), Sharma (RF global) |

---

## 3. The Original CDS Algorithm: Identified Failure Modes

The original CDS (`cds.py`) was a binary classifier producing HEALTHY, UNHEALTHY, or SCREENING predictions. Each design limitation of the original CDS motivated a specific innovation in CDS-OVR. This section traces those limitations to code-level mechanisms.

### 3.1 Hard Healthy Range — Catastrophic False Positive Generation

The original CDS computed per-feature "healthy ranges" as [min(healthy_values), max(healthy_values)] during training (`cds.py`, lines 238–248). During prediction, any single feature value outside this range triggered an immediate UNHEALTHY return (`cds.py`, lines 360–361), bypassing all subsequent evidence accumulation.

This design produces a systematic false positive bias. Consider: with 279 features, each having an independently estimated healthy range from the training set, a test patient has 279 independent opportunities to fall outside at least one range. If the probability of a healthy test patient exceeding the training range on any single feature is p (even as low as 0.01), the probability of at least one exceedance is:

P(≥1 exceedance) = 1 − (1 − p)^279

For p = 0.01: P = 1 − 0.99^279 ≈ 0.94
For p = 0.005: P = 1 − 0.995^279 ≈ 0.75

Even with a per-feature exceedance rate as low as 0.5%, three-quarters of healthy test patients would be incorrectly flagged. The probability p is not negligible because the training-set range is a sample statistic with variance inversely proportional to sample size, extreme quantiles of any distribution are poorly estimated from finite samples.

**CDS-OVR's resolution**: The healthy bar mechanism (`cds_ovr.py`, lines 455–468) replaces the binary hard-range test with a continuous healthy score that interacts with per-class disease thresholds. No single feature can trigger an immediate classification. The healthy score is computed from the same dual-AF evidence accumulation pipeline as disease scores, integrating evidence across all retained features before making a decision.

### 3.2 Unsupervised Equal-Width Binning

The original CDS used Sturges' rule (K = ⌈1 + log₂(n)⌉) to determine bin count and equal-width intervals. This binning is entirely independent of the class labels — it partitions the feature range uniformly regardless of where class boundaries occur. If a clinically significant boundary (e.g., heart rate = 100 bpm separating normal from tachycardic) falls within the interior of a bin, the model cannot detect it.

**CDS-OVR's resolution**: Supervised chi-squared binning (`cds_ovr.py`, lines 209–265) places split points where class composition changes most significantly. Each split maximizes the chi-squared statistic between the target class and all other classes, ensuring that bin boundaries align with actual class boundaries in the data.

### 3.3 Single-Accumulator Evidence and No Multiclass Capability

The original CDS accumulated a single accumulation factor (AF) that could only increase, and produced binary predictions (HEALTHY/UNHEALTHY) without identifying which disease was present. Evidence against a disease hypothesis was structurally invisible — it simply was not accumulated.

**CDS-OVR's resolution**: The One-vs-Rest decomposition trains independent models for each of 8 classes, and the dual-AF mechanism (`cds_ovr.py`, lines 399–442) separately tracks AF_for and AF_against, combining them via ratio scoring: score = (AF_for + ε) / (AF_against + ε). This enables the model to distinguish between strong disease evidence, ambiguous evidence, and strong healthy evidence.

---

## 4. CDS-OVR Architecture: Design Decisions and Their Effects

This section describes each component of CDS-OVR with reference to the source code, explains the design rationale, and traces its effect on classification accuracy.

### 4.1 One-vs-Rest Decomposition with Independent Per-Class Models

**Implementation** (`cds_ovr.py`, lines 370–394): For each of the 8 classes, a separate model is trained by computing `is_target = (nd_labels == target_class)` and running supervised binning, Fisher scoring, and feature selection against this binary partition. During prediction (lines 445–470), each class model produces an independent score, and the highest-scoring disease class exceeding its threshold is selected.

**Design rationale**: OVR with independent per-class pipelines ensures that the feature set, bin boundaries, and decision thresholds for each disease are optimized for that disease alone. This is the central architectural distinction from all benchmarks: **class-awareness at every pipeline stage**, not just at the final classification layer.

**Contrast with coupled architectures**: Mustaqeem's One-Against-One (OAO) SVM creates C(C,2) = 78 pairwise classifiers for the 13 non-empty classes (the dataset has 16 class labels but 3 have zero patients). Each pairwise classifier for rare class k vs. abundant class 1 sees extreme imbalance (e.g., 2 vs. 245 patients for class 8 vs. class 1), and the SVM's margin-maximizing objective pushes the hyperplane close to the rare class, producing biased vote counts. Irfan's CNN-LSTM uses SoftMax output, which enforces Σ P(class_i) = 1 — any increase in the logit for the majority class mechanically suppresses probabilities for all minority classes, even when disease evidence is strong.

CDS-OVR's per-class scores are computed from separate models with no mathematical coupling. The class 2 score of 4.5 and the class 10 score of 6.2 are derived from entirely different feature sets, bin boundaries, and posterior estimates. The only inter-class interaction is through the healthy bar mechanism, which is designed to prevent false positives by raising disease thresholds when healthy evidence is strong.

### 4.2 Supervised Chi-Squared Binning

**Implementation** (`cds_ovr.py`, lines 209–265): For each continuous feature in each class model, the algorithm iteratively selects the split point maximizing the Pearson chi-squared statistic between the target-class indicator and the resulting bin assignment. Constraints prevent overfitting: each side of a split must contain ≥ MIN_SUPPORT patients, the chi-squared gain must exceed 0.5, and at most MAX_BINS = 6 bins are created.

**Effect on evidence quality**: Supervised bin boundaries produce bins with more homogeneous class composition than equal-width bins.

> **Experiment: Supervised vs. Equal-Width Binning Quality** (`evidence_analysis.py`, Section 9)
>
> **What was tested**: For the same set of retained features, posterior shifts (|p_class[bin] − prior|) were computed using (A) CDS-OVR's supervised chi-squared bins and (B) equal-width bins using Sturges' rule (K = ⌈1 + log₂(n)⌉).
>
> **Why**: To quantify whether supervised binning actually produces stronger class signals than unsupervised binning — i.e., whether placing bin edges at class boundaries concentrates target patients into fewer bins with higher posteriors.
>
> **Method**: Using full training data (10-fold CV, seed=13), each feature's bin edges were computed both ways. For each bin, the Laplace-smoothed posterior `p_class[b] = (target_counts[b] + α · prior) / (bin_counts[b] + α)` was computed, and the average absolute posterior shift across all bins was compared between the two methods.
>
> **Results**:
> - Class 6 (Sinus Bradycardia): supervised average |posterior shift| = 0.253 vs. equal-width = 0.102 → **2.5× stronger signal**
> - Class 10 (RBBB): supervised = 0.482 vs. equal-width = 0.441 → 1.1× stronger signal
> - Class 6 best feature (feature 14, heart rate, FDR=2.526): supervised creates a bin with posterior = 0.754, concentrating 12/15 target patients (prior = 6.0%) → **69.4 pp posterior shift**. Equal-width bins spread these 12 patients across more bins, diluting the signal.
>
> **Interpretation**: Supervised binning's advantage is most pronounced for classes with concentrated, clinically distinct feature ranges (class 6: heart rate < 60 bpm creates a natural bin). For classes spread across broader feature ranges (class 10: RBBB has multiple morphological signatures), the advantage is smaller. The 2.5× stronger signal for class 6 translates directly to faster evidence accumulation during prediction — fewer features are needed to exceed the disease threshold, making the model more robust to individual feature noise.

Within a supervised bin, the Laplace-smoothed posterior is a more accurate estimate of the true class probability, producing more decisive evidence signals.

**Regularization against overfitting**: The chi-squared gain threshold of 0.5 prevents the creation of bins driven by sampling noise rather than genuine class structure. The MAX_BINS = 6 cap limits model complexity per feature. Combined with the prediction-time bin count filter (`bc < 3 → skip`), these constraints ensure that evidence is only accumulated from bins with sufficient statistical support.

### 4.3 Dual Accumulation Factor with Ratio Scoring

**Implementation** (`cds_ovr.py`, lines 399–442): For each retained feature in a class model, the patient's value is binned, the posterior shift `p_class[b] − prior` is computed, weighted by confidence (min(1, bin_count/10)) and Fisher weight (√(fisher/max_fisher), floored at 0.1), and added to either AF_for (positive shift) or AF_against × against_scale (negative shift). The final score is (AF_for + 0.1) / (AF_against + 0.1).

**Why ratio scoring is more informative than single-accumulator methods**: A single accumulator that only increases (original CDS) cannot represent the case where 5 features provide evidence against a disease and 3 features provide evidence for it — the accumulator simply reaches some intermediate value, indistinguishable from 3 features providing moderate for-evidence and 0 against.

The ratio score creates an interpretable scale:
- score ≈ 1.0: balanced for/against evidence — ambiguous
- score >> 1.0: dominant for-evidence — likely disease
- score << 1.0: dominant against-evidence — likely not disease

> **Experiment: Ratio Score Separation Between Correct and Incorrect Predictions** (`evidence_analysis.py`, Section 10)
>
> **What was tested**: After running 10-fold CV (seed=13) on all 416 patients, the AF_for, AF_against, and ratio scores of the winning class model were extracted for every patient and grouped by whether the prediction was correct or incorrect.
>
> **Why**: To verify that the dual-AF ratio score produces meaningfully different values for correct vs. incorrect predictions — i.e., that the scoring mechanism has discriminative power and that failures correlate with ambiguous (ratio ≈ 1) rather than confidently wrong (ratio >> 1) scores.
>
> **Method**: For each of the 416 patients, the class model that produced the highest score (or class 1 if no disease threshold was exceeded) was identified. The AF_for, AF_against, and final ratio = (AF_for + 0.1)/(AF_against + 0.1) were recorded. Patients were split into correct (predicted == true label) and incorrect groups.
>
> **Results**:
> - **Correctly classified (361/416 patients)**: mean ratio = 6.859, median = 4.545; mean AF_for = 1.389, mean AF_against = 0.241
> - **Incorrectly classified (55/416 patients)**: mean ratio = 1.664, median = 1.247; mean AF_for = 0.505, mean AF_against = 0.565
>
> **Interpretation**: The separation is stark. Correct predictions show AF_for >> AF_against (ratio well above 1.0), meaning multiple features concordantly support the winning class. Wrong predictions show balanced accumulation (AF_for ≈ AF_against, ratio near 1.0), meaning the model is genuinely uncertain — features are sending mixed signals. A single accumulator (as in the original CDS) would show moderate values in both cases and cannot distinguish "strong evidence for the right answer" from "weak evidence pointing in multiple directions." The dual-AF architecture makes model uncertainty visible and actionable.

The epsilon term (ε = 0.1) is critical: it prevents division by zero and ensures that AF_for must exceed approximately ε × threshold ≈ 0.3 to trigger classification, requiring multiple concordant feature signals rather than a single feature with strong but potentially unreliable evidence.

### 4.4 Fisher Discriminant Ratio Feature Weighting

**Implementation** (`cds_ovr.py`, lines 316–321, applied at line 430): For each feature in each class model, the Fisher discriminant ratio FDR = (μ_target − μ_rest)² / (σ²_target + σ²_rest) is computed. During evidence accumulation, each feature's contribution is scaled by fw = max(√(FDR_f / max_FDR), 0.1).

**Design rationale**: Not all features contribute equally to class discrimination. A feature where the target class has a distinct mean (large numerator) and tight distribution (small denominator) is more informative than a feature with overlapping distributions. Fisher weighting ensures that the evidence score is dominated by the most discriminative features for each specific class.

> **Experiment: Per-Class FDR Specificity — Same Feature, Different Importance** (`evidence_analysis.py`, Section 5)
>
> **What was tested**: For each of the top 5 features per class (ranked by FDR), the same feature's FDR was computed across all 8 classes to measure how class-specific each feature's discriminative power is.
>
> **Why**: If the same feature has high FDR for all classes, per-class weighting adds no benefit over global weighting. If FDR varies dramatically across classes for the same feature, this proves that features are class-specific and a global weight would be incorrect for most classes.
>
> **Method**: For all 279 features, FDR = (μ_target − μ_rest)² / (σ²_target + σ²_rest) was computed 8 times (once per class). The top features by FDR for each class were identified, and their FDR values for other classes were compared.
>
> **Results**:
> - Feature 111: FDR = **19.471 for class 3** (Old Anterior MI) but < 0.1 for classes 1, 5, 6, 9 — a **194× ratio** between most and least discriminative classes
> - Feature 16: FDR = **18.526 for class 9** (LBBB) but < 0.3 for all other classes
> - Feature 14 (heart rate): FDR = **3.303 for class 5** (tachycardia) and **2.526 for class 6** (bradycardia) but < 0.5 for classes 3, 4, 10
> - No single feature ranks in the top 5 for more than 2 classes
>
> **Interpretation**: The extreme class-specificity of FDR values (194× variation for feature 111) proves that a global feature weighting scheme assigns incorrect importance for most classes. Feature 111 should dominate class 3's decision but be effectively ignored for class 5. CDS-OVR's per-class Fisher weighting achieves this automatically; global methods (Sharma's Pearson, Mustaqeem's RF importance, Hybrid FS's RFE) compute a single importance value that averages across all classes, severely underweighting features critical for rare diseases.

**Per-class specificity**: The experiment above demonstrates extreme class-specificity: feature 111 has FDR = 19.471 for class 3 but < 0.1 for most other classes (194× ratio); feature 16 has FDR = 18.526 for class 9 but < 0.3 for all others. This per-class weighting is structurally impossible in methods that use a single global feature set (Sharma, Gupta, Hybrid FS) or global feature importance (Mustaqeem's RF-based wrapper).

### 4.5 Correlation-Based Feature Filtering

**Implementation** (`cds_ovr.py`, lines 330–367): For each class model, the top 3 × FPC (54) features ranked by action score are considered. Features are greedily selected subject to the constraint that no selected feature has |correlation| > 0.8 with any previously selected feature, up to FPC = 18 features per class.

**Effect**: This prevents the model from spending multiple feature slots on redundant measurements. In ECG data, where the same waveform component is measured across 12 leads, this is particularly important. Without correlation filtering, a class 10 (RBBB) model might select QRS duration measurements from 5 correlated leads, all telling the same story, wasting 4 slots that could detect T-wave inversion, axis deviation, or R-wave amplitude patterns.

> **Experiment: CDS-OVR Within-Branch Correlation Enforcement** (`evidence_analysis.py`, Section 11)
>
> **What was tested**: After training CDS-OVR on full data (10-fold CV, seed=13), the maximum pairwise correlation within each class model's retained feature set was measured, both within individual sex branches (where the 0.8 threshold is enforced) and across the combined feature set (both branches pooled).
>
> **Why**: To verify that CDS-OVR's correlation filtering actually maintains non-redundancy within each branch, and to characterize the residual correlation when features from both branches are pooled.
>
> **Method**: For each of 8 class models: (a) within each sex branch, compute max pairwise |r| among the ≤18 retained features — should be < 0.8 by construction. (b) Pool features from both branches and compute max pairwise |r| — may exceed 0.8 because filtering is per-branch.
>
> **Results**:
> - Within-branch max |r|: all classes ≤ 0.789 (threshold 0.8 holds as designed)
> - Cross-branch max |r|: classes 1, 2, 4, 5 reach up to 0.895 (features filtered independently within each branch may correlate across branches)
> - Class 9 (no effective sex branching due to small size): max |r| = 0.789, well below threshold
>
> **Interpretation**: The correlation threshold works as designed within each branch. The cross-branch correlation (up to 0.895) is an expected consequence of independent per-branch selection — a feature important for males and a correlated feature important for females will both be retained. This is a minor inefficiency (at most 2–3 cross-branch pairs per class exceed 0.8) compared to Sharma's 32 highly correlated pairs with no filtering at all.

**Contrast with Sharma's Pearson filtering**: Sharma 2024 selects features whose Pearson correlation with the target variable exceeds 0.1, retaining 101 of 279 features. This threshold is very low — features with |r| = 0.1 explain only 1% of the variance in the target. More importantly, this is a single global feature set applied to the RF classifier. Sharma does not report removing inter-feature correlation (their heatmap in Figure 3 shows substantial residual correlation among the 101 selected features — 32 pairs exceed |r| > 0.8, Section 2.2). CDS-OVR's approach selects fewer features (18 per class) but ensures they are both discriminative (ranked by chi-squared score and FDR) and non-redundant (pairwise |r| < 0.8 per branch, as verified in the experiment above).

### 4.6 Per-Class Imbalance Adaptation

**Implementation** (`cds_ovr.py`, lines 42–44):

| Parameter | Rare classes {4, 5, 9} | Common classes |
|-----------|----------------------|----------------|
| MIN_SUPPORT (training) | 2 | 3 |
| CONF_SUPPORT (training) | 5 | 10 |
| against_scale (prediction) | 0.5 | 0.8 |

During prediction, all classes use hardcoded MIN_SUPPORT = 3 and CONF_SUPPORT = 10 (lines 425–429).

**Rationale for the training/prediction asymmetry**: Training with MIN_SUPPORT = 2 allows the discovery of potentially discriminative features for rare classes — features where even 2 patients concentrate in the same bin. Prediction with MIN_SUPPORT = 3 ensures that evidence is only accumulated from bins with sufficient sample support, preventing unreliable posterior estimates from driving the ratio score.

**against_scale = 0.5 for rare classes**: This is the most distinctive imbalance handling mechanism in CDS-OVR. It encodes a principled asymmetry: with only 9–15 patients in the rare classes {4, 5, 9}, the model's estimate of "what a non-class-k patient looks like" is based on ~400 patients from 7 other classes, which is a diverse population with high variance. A feature bin showing negative evidence (p_class < prior) could be negative simply because the bin contains a mix of other disease classes, not because the target class is absent. Downweighting against-evidence by 50% reflects this greater uncertainty in the meaning of negative evidence for rare classes.

> **Ablation Experiment: against_scale effect on rare-class detection** (`evidence_ablation.py`, Section 1)
>
> **What was tested**: CDS-OVR was run in two configurations: (A) the current design with against_scale=0.5 for rare classes {4, 5, 9} and 0.8 for common classes, vs. (B) a uniform against_scale=0.8 for all classes.
>
> **Why**: To quantify whether the reduced against-penalty for rare classes actually improves their detection, or whether the overall accuracy difference is negligible.
>
> **Method**: Both configurations were evaluated using 10-fold cross-validation across 10 random seeds (seeds 13–76), giving 100 total fold evaluations per configuration. The only parameter changed between runs was AGAINST_SCALE_MAP; all other settings (features, bins, thresholds) were identical.
>
> **Results**:
> - Overall accuracy: current 84.66% vs. uniform 84.59% (+0.07 pp, 5/10 seeds favoring current)
> - **Class 4 (Old Inferior MI, 15 patients)**: current 69.3% vs. uniform 62.7% → **+6.7 pp improvement**
> - **Class 5 (Sinus Tachycardia, 13 patients)**: current 59.2% vs. uniform 50.0% → **+9.2 pp improvement**
> - **Class 9 (LBBB, 9 patients)**: current 96.7% vs. uniform 95.6% → +1.1 pp improvement
>
> **Interpretation**: The against_scale=0.5 mechanism has negligible effect on overall accuracy (+0.07 pp) because rare classes collectively represent only 37/416 = 8.9% of patients. However, it produces substantial improvements in rare-class detection: +9.2 pp for class 5 and +6.7 pp for class 4. This confirms the mechanism works as designed — it prevents negative evidence (accumulated from the heterogeneous non-target population of ~400 patients) from suppressing weak positive evidence in classes where positive evidence is inherently sparse. The trade-off is that common classes may very occasionally misclassify toward rare classes, but this effect is too small to register in overall accuracy.

**No benchmark implements per-class parametrization.** Sharma's RF uses the same `min_samples_split = 2` and `gini` criterion for all trees; Mustaqeem uses the same SVM kernel and regularization for all 78 pairwise classifiers; Irfan uses the same CNN-LSTM architecture and SoftMax for all 13 classes.

### 4.7 Dynamic Healthy Bar and Suspicion Mechanism

**Implementation** (`cds_ovr.py`, lines 455–468):

```
h_score = class_scores[HEALTHY]    # ratio score from the healthy (class 1) model
healthy_bar = min(1.05 × h_score, 5.0)

For each disease class:
  threshold = CLASS_THRESHOLDS[cls]   # e.g., 3.5 for class 2, 3.0 for class 10
  if h_score < 2.0:                  # suspicion mechanism
      threshold -= 0.3
  threshold = max(threshold, healthy_bar)
  if disease_score > threshold → candidate
```

**Design rationale**: The healthy bar creates an adaptive interaction between healthy evidence and disease evidence.

> **Experiment: Healthy Bar Sensitivity/Specificity Analysis** (`evidence_analysis.py`, Section 13)
>
> **What was tested**: In 10-fold CV (seed=13), all 416 patients were classified and the results were partitioned by true class (healthy vs. disease) to compute the healthy bar's sensitivity and specificity.
>
> **Why**: To measure how effectively the healthy bar prevents false positives (healthy patients incorrectly flagged as diseased) while maintaining disease detection (diseased patients correctly identified as having some condition).
>
> **Method**: After 10-fold CV produces predictions for all 416 patients, each patient is categorized as: (a) healthy correctly classified as healthy, (b) healthy incorrectly classified as diseased (false positive), (c) diseased correctly identified as having some disease (even if wrong disease), (d) diseased incorrectly classified as healthy (false negative).
>
> **Results**:
> - **Specificity**: 224/245 healthy patients correctly classified = **91.4%** (21 false positives, 8.6% FPR)
> - **Disease detection rate**: 145/171 disease patients identified as diseased = **84.8%** (137 correct class + 8 wrong disease class; 26 missed as healthy)
> - False negatives (26 disease patients predicted healthy) break down as: 8 from class 10 (RBBB), 10 from class 2 (CAD), 3 from class 4 (Old Inferior MI), 5 from class 5 (Sinus Tachycardia)
>
> **Interpretation**: The healthy bar achieves 91.4% specificity — for every 100 healthy patients, only ~9 are incorrectly flagged. This is achieved while detecting 84.8% of disease patients. The 26 missed disease patients are concentrated in classes where disease evidence overlaps with healthy evidence (class 10: RBBB patients with borderline QRS widening; class 2: CAD with subtle ischemic changes). The healthy bar's dynamic threshold (scaling with h_score) correctly protects healthy patients without a fixed cutoff that would uniformly suppress all disease evidence.

For a patient with strong healthy evidence (h_score = 4.0), the effective threshold becomes max(CLASS_THRESHOLD, 4.2), requiring very strong disease evidence to override. For a patient with weak healthy evidence (h_score = 1.5, triggering suspicion), thresholds are lowered by 0.3, increasing sensitivity to disease detection.

**CLASS_THRESHOLDS[10] = 3.0**: This value was set based on error analysis of earlier model versions. Class 10 (RBBB, 50 patients) showed systematic under-detection. With the current threshold of 3.0, per-class accuracy in 10-fold CV (seed=13, `evidence_analysis.py`, Section 14): class 1: 91.4% (224/245), class 2: 77.3% (34/44), class 3: 93.3% (14/15), class 4: 73.3% (11/15), class 5: 61.5% (8/13), class 6: 84.0% (21/25), class 9: 88.9% (8/9), class 10: 82.0% (41/50). The confusion matrix shows class 10's primary error mode is misclassification to class 1 (8 RBBB patients predicted as Normal), confirming that the healthy bar sometimes overrides disease evidence for borderline RBBB cases.

Kian's Note: While the explanations provided for changes is solid, when it comes to constants especially, this document explains how it works as opposed to how the value is selected. With other design decisions, including the healthy bar, it was only generally explained why it is useful instead of explaining where its benefits come in practice. The numerical evidance is fine.

---

## 5. Benchmark-by-Benchmark Analysis

### 5.1 Sharma et al. 2024 — Optimized Random Forest

**Published in**: Engineering Research Express, Volume 6, 035209 (2024)

**Method pipeline**:
1. Identify and remove rows with missing values (452 → 420 instances)
2. Drop column 14 (>80% missing values)
3. Pearson correlation-based feature selection: retain features with |r| > 0.1 with target → 101 features (Sharma's reported count; our simulation yields 106 on 416 patients/279 features and 108 when simulating Sharma's pipeline on 394 patients/278 features — `evidence_ablation.py`, Section 4. The remaining discrepancy of 7 features is explained by Sharma using all 452 patients including 5 additional classes, which shifts the binary correlations)
4. Normalize features
5. Train 6 ML models (LR, XGB, LDA, GNB, SVM, RF), each optimized via Grid Search
6. Evaluate on three splits: 70/30 (DS1), 80/20 (DS2), 90/10 (DS3)
7. Binary classification only (Normal vs. Arrhythmia)

**Best result**: RF on DS3 (90/10): TP=17, FP=0, FN=2, TN=23 → 95.24% accuracy, 100% precision, 89.47% sensitivity, 100% specificity.

**CDS-OVR comparison**: Binary 90/10 best = 97.6% (seed=76: TP=14, FN=1, TN=27, FP=0 — 1 error out of 42 test patients), mean = 88.6% across 10 seeds.

#### 5.1.1 Statistical Fragility of Sharma's 95.24%

Sharma's 90/10 split produces a test set of **42 patients** (23 normal, 19 arrhythmia per their reported confusion matrix: TN=23, TP=17, FN=2, FP=0). The reported 95.24% accuracy corresponds to exactly **2 misclassifications** out of 42. This creates extreme sensitivity to individual patients:

- 2 errors / 42 = 95.24%
- 3 errors / 42 = 92.86%
- 1 error / 42 = 97.62%

A single additional misclassification changes the accuracy by 2.38 pp. Without variance reporting across multiple splits, it is impossible to distinguish whether Sharma's 95.24% represents the method's expected performance or a favorable draw. CDS-OVR's 10-seed evaluation on the same 90/10 protocol shows binary accuracy ranging from 81.0% to 97.6% (σ=4.74%, `run_all_splits.py`), demonstrating that single-split results on 42 test patients are unreliable indicators of true model performance.

#### 5.1.2 Where CDS-OVR Structurally Outperforms Sharma's RF — Feature Selection

Sharma selects 101 features globally using Pearson correlation with a threshold of |r| > 0.1. This approach has three structural weaknesses relative to CDS-OVR:

> **Experiment: Noise Feature Analysis in Sharma's Selection** (`evidence_analysis.py`, Sections 3, 7)
>
> **What was tested**: (A) How many of Sharma's retained features explain minimal target variance (r² < 0.02). (B) How many features would pass the |r| > 0.1 threshold by chance alone under the null hypothesis of no association.
>
> **Why**: To determine whether Sharma's permissive threshold (|r| > 0.1) retains features that are noise rather than signal — features that pass the correlation test by chance or explain so little variance that they add prediction variance without improving discrimination.
>
> **Method**: (A) For each of the 279 features, Pearson correlation with the binary target (normal=0, arrhythmia=1) is computed using the 416-patient CDS-OVR dataset. Features passing |r| > 0.1 are retained, and their r² (proportion of variance explained) is tabulated. (B) Under H₀ of no association between feature and target, the sampling distribution of |r| with n=420 samples was computed. The expected number of features exceeding |r| > 0.1 by chance = 279 × P(|r| > 0.1 | H₀, n=420).
>
> **Results**:
> - 106 features pass |r| > 0.1 in our simulation (Sharma reports 101; the discrepancy of 5 features is due to different patient sets — see `evidence_ablation.py`, Section 4: simulating Sharma's exact pipeline on 394 patients yields 108 features)
> - Of these 106: **42 features have r² < 0.02** (explain less than 2% of variance), and **51 features fall in the borderline range |r| ∈ [0.1, 0.15]**
> - Under H₀: **~11.4 features expected to exceed |r| > 0.1 by chance** with 420 samples
>
> **Interpretation**: Nearly half of Sharma's retained features (42/106) explain less than 2% of target variance — their contribution to an RF classifier is dominated by sampling noise. Approximately 11 of the 106 features are statistically indistinguishable from chance associations. These noise features force the RF to learn spurious splits that don't generalize, adding variance without improving the signal-to-noise ratio of the overall model.

**Weak threshold retains noise features.** Features with |r| = 0.1 explain only 1% of target variance (r² = 0.01). The experiment above shows that 42 of Sharma's 106 retained features explain less than 2% of variance, and ~11.4 features would pass the threshold by chance alone.

**Global feature set cannot optimize for individual diseases.** Pearson correlation with the binary target (normal=0, arrhythmia=1) measures each feature's linear association with the combined disease label. A feature that perfectly separates sinus bradycardia from normal (e.g., heart rate < 60) but is irrelevant for all other diseases contributes only 25/207 = 12% of the disease-class variance. Its Pearson |r| with the binary target is diluted by the 182 non-bradycardia disease patients for whom this feature is uninformative. If diluted below 0.1, it is discarded entirely.

> **Experiment: Feature Set Divergence Across Class Models** (`evidence_analysis.py`, Section 4)
>
> **What was tested**: After training CDS-OVR on the full dataset, the retained feature sets for each of the 8 class models were extracted and compared pairwise using Jaccard similarity (|intersection|/|union|).
>
> **Why**: To quantify whether per-class feature selection actually produces *different* feature sets per disease — if all class models converge on the same features, per-class selection adds complexity without benefit. Low Jaccard overlap proves that different diseases require genuinely different feature subsets.
>
> **Method**: CDS-OVR's training phase selects up to 18 features per branch for each class model (up to 36 total per class with sex branching). After training on full data (10-fold CV, seed=13), the complete retained feature set for each class was extracted. Pairwise Jaccard overlap = |features_A ∩ features_B| / |features_A ∪ features_B| was computed for all 28 class pairs. Unique features (used by only one class) were also counted.
>
> **Results**:
> - Total unique features across all 8 models: **109** (out of possible 144 slots = 8 × 18)
> - Pairwise Jaccard overlap: **min = 0.023** (class 3 vs. class 10), **max = 0.289** (class 4 vs. class 6)
> - Features unique to single class models: class 10 (RBBB) = **13 unique features** (e.g., features 56, 65, 89, 90, 92 — QRS morphology); class 3 (AM) = **8 unique features** (e.g., features 99, 111, 112 — Q-wave indicators); class 4 = 10 unique; class 2 = 7 unique
>
> **Interpretation**: The extremely low overlap (Jaccard as low as 0.023 = barely 2% shared) proves that different diseases genuinely require different features. Class 10's 13 unique features are specific to RBBB morphology; class 3's 8 unique features are specific to Q-wave/ST-segment changes from prior MI. A global feature set of 32 (Hybrid FS) or even 101 (Sharma) features cannot simultaneously include all 13 RBBB-specific features AND all 8 AM-specific features AND all features for 6 other diseases without also including substantial noise. CDS-OVR's per-class approach ensures each disease gets exactly the features it needs.

**No inter-feature redundancy removal.** Sharma's Figure 3 (inter-variable correlation matrix) shows substantial correlation among selected features: 32 pairs exceed |r| > 0.8 (`evidence_analysis.py`, Section 3). CDS-OVR's CORR_THRESHOLD = 0.8 ensures that within each tree node (sex branch), no two retained features share more than 64% of their variance. When features from both sex branches are pooled per class, the combined set can have higher maximum correlations — up to 0.895 for classes 1, 2, 4, 5 (`evidence_analysis.py`, Section 11) — because features filtered independently within each branch may correlate across branches. Within a single branch's 18-feature set, the threshold holds: class 9 (no sex branching effect) achieves max |r| = 0.789, below threshold. The contrast with Sharma remains: Sharma retains 32 highly correlated pairs globally with no filtering mechanism at all.

#### 5.1.3 Where CDS-OVR Structurally Outperforms Sharma's RF — Missing Value Handling

Sharma drops column 14 entirely (>80% missing) and removes rows with remaining missing values (452 → 420, losing 32 patients / 7.1% of the data). This approach has two costs:

> **Experiment: Simulating Sharma's Row-Deletion Impact** (`evidence_analysis.py`, Section 2)
>
> **What was tested**: Sharma's preprocessing pipeline was simulated on the CDS-OVR dataset: drop column 14 (feature 13, >80% missing), then remove any row with remaining missing values.
>
> **Why**: To quantify whether row deletion disproportionately removes minority-class patients, which would systematically reduce training data for already-small classes.
>
> **Method**: Starting with 416 patients: (1) delete feature 13 (column 14), (2) identify patients with any remaining NaN in features 10, 11, or 12, (3) count removed patients by class.
>
> **Results**: After dropping column 14, 22 patients still have missing values and are removed. Removal rate by class:
> - Class 2 (CAD): **8/44 removed (18.2%)**
> - Class 3 (AM): 2/15 removed (13.3%)
> - Class 1 (Normal): 8/245 removed (3.3%)
>
> **Interpretation**: Class 2 loses 18.2% of its patients — nearly 1 in 5 — vs. only 3.3% for the majority class. This 5.5× disparity means Sharma's RF trains with a class 2 depleted from 44 to 36 patients, exacerbating the imbalance between normal (237 remaining) and CAD (36 remaining) from 5.6:1 to 6.6:1.

**Lost patients are not randomly distributed.** The experiment above shows class 2 (CAD) loses 18.2% of its patients under Sharma's preprocessing, while class 1 (Normal) loses only 3.3%. This disproportionate loss depletes minority classes that are already data-starved.

**Column 14 carries diagnostic information.** Feature 13 (0-indexed; Sharma's column 14) has 83.9% missing values, but it is not uninformative — its Fisher discriminant ratio reaches 1.437 for class 3 (Old Anterior MI), ranking it #19 out of 279 features by FDR for that class (`evidence_analysis.py`, Section 12). While not among the top features, it exceeds the FDR of many retained features. CDS-OVR's class 2 model uses feature 13 directly in its retained feature set (evidence report §4). Deleting this feature removes signal that CDS-OVR exploits.

CDS-OVR handles missing values by skipping them at prediction time (`if np.isnan(v): continue` in `_compute_af()`, line 417). No patients are discarded, no features are deleted. When a feature value is available, it contributes to the evidence; when missing, it is simply omitted from the sum. This preserves all 416 patients and all 279 features.

#### 5.1.4 Where CDS-OVR Structurally Outperforms Sharma's RF — Decision Architecture

Random Forest makes predictions by majority vote across an ensemble of decision trees. Each tree partitions the feature space recursively, selecting the best split at each node based on Gini impurity across all classes globally. For binary classification with RF:

- All trees share the same feature space (101 features).
- Each tree samples features randomly at each split (max_features = sqrt, i.e., ~10 features per split).
- The final prediction is the majority vote across all trees.

This architecture has no mechanism to adapt decision sensitivity per disease class. A patient with a rare arrhythmia (e.g., sinus tachycardia affecting 13 patients) will only be correctly classified if enough trees happen to sample the 1–2 features that distinguish tachycardia from normal, and if those features produce splits that survive the Gini criterion. In a binary model, all arrhythmias are lumped together — the RF cannot learn that different diseases require attention to different features.

CDS-OVR's OVR decomposition with per-class feature selection, Fisher weighting, and adaptive thresholds provides exactly this disease-specific attention. The binary accuracy achieved by CDS-OVR (97.6% best) is a byproduct of its multiclass architecture — it correctly identifies *which* disease each patient has, and binary accuracy follows from the fact that correctly classified disease patients are also correctly classified as "not normal."

#### 5.1.5 Where Sharma's RF May Have Advantages

**Feature interactions via tree ensembles.** RF captures conjunctive patterns through path depth: a tree can split on QRS duration > 120 ms at one node and right axis > 90° at a child node, effectively implementing the conjunction "QRS > 120 AND axis > 90°" that indicates RBBB. CDS-OVR evaluates features independently and cannot capture such interactions.

**Implicit dimensionality reduction via bagging.** Each tree sees a bootstrap sample of patients and a random subset of features, providing natural regularization against overfitting. CDS-OVR's regularization operates through different mechanisms (MIN_SUPPORT, chi-squared gain threshold, MAX_BINS cap).

**Zero false positives on this specific split.** Sharma's RF achieved FP = 0 (100% specificity) on their particular 90/10 split. While this may reflect favorable test-set composition, it indicates that RF with careful feature selection can achieve high specificity on this dataset. CDS-OVR's healthy bar mechanism is designed for the same purpose but operates through a different mechanism (continuous threshold scaling rather than tree-based decision rules).

#### 5.1.6 Sharma's Own Split-Dependent Results Reveal Variance

Sharma reports RF accuracy across three splits (their Tables 20–22):

| Split | Accuracy | FP | FN | Test size |
|-------|----------|----|----|-----------|
| 70/30 | 81.75% | 2 | 21 | 126 |
| 80/20 | 84.52% | 2 | 11 | 84 |
| 90/10 | 95.24% | 0 | 2 | 42 |

The jump from 84.52% (80/20) to 95.24% (90/10) — a 10.72 pp improvement — is disproportionately large relative to the jump from 81.75% (70/30) to 84.52% (80/20, +2.77 pp). This non-linear improvement is consistent with two factors: (a) the smaller test set at 90/10 has higher variance, and (b) with only 42 test patients, favorable draws produce outsized accuracy changes. The 70/30 result (81.75%, N=126) is a more stable estimate of Sharma's RF performance because it is computed on 3× as many test patients.

**Direct split-matched comparison** (CDS-OVR binary accuracy from `run_all_splits.py`, 10 seeds each):

| Split | Sharma RF (binary) | CDS-OVR Best (binary) | CDS-OVR Mean (binary) | CDS-OVR σ |
|-------|-------------------|----------------------|----------------------|-----------|
| 70/30 | 81.75% | 90.4% | 85.1% | 4.04% |
| 80/20 | 84.52% | 94.0% | 88.0% | 3.35% |
| 90/10 | 95.24% | 97.6% | 88.6% | 4.74% |

At every split ratio, CDS-OVR's **mean** binary accuracy exceeds Sharma's reported value at 70/30 and 80/20 (+3.4 pp and +3.5 pp respectively). At 90/10, Sharma's 95.24% falls between CDS-OVR's mean (88.6%) and best (97.6%), landing within CDS-OVR's distribution (σ=4.74%). The consistent advantage at 70/30 and 80/20 — where test sets are large enough to reduce variance (N=125 and N=84) — provides stronger evidence of a genuine structural advantage than the 90/10 comparison alone.

### 5.2 Mustaqeem et al. 2018 — SVM with One-Against-One

**Published in**: Computational and Mathematical Methods in Medicine, 2018

**Method pipeline**:
1. Remove binary features (279 → ~206)
2. Wrapper feature selection via Boruta/Random Forest with shadow variables: 206 → 94 features
3. Shadow augmentation: each record duplicated ~5× → 452 → 2,260 records
4. Z-score normalization (zero mean, unit variance)
5. Train SVM with polynomial kernel (degree unspecified, γ = 0.001), OAO decomposition → 120 classifiers
6. Evaluate on five splits: 50/50 through 90/10

**Results across splits** (their Table 1):

| Split | OAO Accuracy | Δ from prior split |
|-------|-------------|-------------------|
| 50/50 | 73.45% | — |
| 60/40 | 74.59% | +1.14 |
| 70/30 | 77.20% | +2.61 |
| 80/20 | 81.11% | +3.91 |
| 90/10 | 92.07% | +10.96 |

**Key observation**: The 50/50 → 90/10 improvement of +18.62 pp indicates high sensitivity to training data volume. The disproportionate jump from 80/20 to 90/10 (+10.96 pp) suggests the model is data-hungry — likely because the 78 pairwise classifiers each require sufficient examples from both classes, and at 80/20 several pairwise classifiers involving rare classes still lack support.

**Direct split-matched comparison** (CDS-OVR multiclass accuracy from `run_all_splits.py`, 10 seeds each):

| Split | Mustaqeem OAO (13 non-empty classes) | CDS-OVR Best (8-class) | CDS-OVR Mean (8-class) | CDS-OVR σ |
|-------|-------------------------|----------------------|----------------------|-----------|
| 50/50 | 73.45% | 85.1% | 79.4% | 2.85% |
| 60/40 | 74.59% | 86.2% | 80.2% | 3.06% |
| 70/30 | 77.20% | 87.2% | 81.4% | 3.75% |
| 80/20 | 81.11% | 89.3% | 85.5% | 2.43% |
| 90/10 | 92.07% | 92.9% | 86.2% | 4.97% |

CDS-OVR's **mean** multiclass accuracy exceeds Mustaqeem's at every split from 50/50 through 80/20 by 4.2–6.0 pp. At 90/10, Mustaqeem's 92.07% exceeds CDS-OVR's mean (86.2%) but falls within CDS-OVR's best (92.9%) — the difference is within single-split variance. The consistent advantage at lower splits, where test sets are larger and variance is lower, is more informative than the 90/10 comparison. Note: Mustaqeem classifies 13 non-empty classes (16 labels, 3 empty — including classes with as few as 2 patients), while CDS-OVR classifies 8 classes after removing 5 classes with insufficient representation — different class counts make direct comparison imperfect, but CDS-OVR's advantage at 50/50–80/20 is too large to explain by class reduction alone.

#### 5.2.1 Why CDS-OVR Matches Mustaqeem at 90/10

CDS-OVR best 90/10 multiclass: 92.9% vs. Mustaqeem 92.07%. The difference (0.83 pp) is within the variance of single-split evaluation.

The structural reason CDS-OVR matches Mustaqeem despite its simpler classification model (no kernel methods, no feature interactions) is that CDS-OVR's per-class optimization compensates for its lower model expressiveness. Two specific mechanisms:

> **Experiment: OAO Pairwise Imbalance Analysis** (`evidence_analysis.py`, Section 1)
>
> **What was tested**: For all C(13,2) = 78 One-Against-One classifier pairs in Mustaqeem's SVM-OAO architecture, the class imbalance ratio of each pair was computed.
>
> **Why**: SVM margin-maximizing objective is sensitive to class imbalance — with extreme ratios, the hyperplane shifts toward the minority class support vectors, biasing predictions toward the majority class. Quantifying how many pairs are severely imbalanced reveals how many of Mustaqeem's 78 classifiers produce biased vote counts.
>
> **Method**: Using the UCI Arrhythmia dataset's 13 non-empty class counts (from all 452 patients as Mustaqeem uses), the ratio max(n_i, n_j)/min(n_i, n_j) was computed for each of the 78 pairs. Pairs with ratio > 10:1 were flagged as severely imbalanced.
>
> **Results**:
> - **17 of 78 pairs (21.8%) have imbalance ratios exceeding 10:1**
> - Worst case: class 1 (Normal, 245) vs. class 8 (2 patients) = **122.5:1**
> - Class 1 vs. class 9 (LBBB, 9 patients) = **27.2:1**
> - Class 1 vs. class 5 (Sinus Tachycardia, 13 patients) = **18.8:1**
> - Class 1 vs. class 3 (AM, 15 patients) = **16.3:1**
>
> **Interpretation**: Over one-fifth of Mustaqeem's pairwise classifiers face extreme imbalance. In these 17 pairs, the SVM's margin-maximizing objective shifts hyperplanes toward the minority class, producing classifiers biased toward predicting the majority class of each pair. During OAO voting, this bias accumulates: class 1 (Normal) wins its biased pairwise votes against rare classes, receiving inflated vote counts. This structural bias explains why Mustaqeem's OAO accuracy jumps +10.96 pp from 80/20 to 90/10 — more training data per pair reduces the imbalance effect, and at 90/10 even the rarest pairs have enough support vectors to form a reasonable margin.

1. **Per-class feature selection vs. global wrapper FS**: Mustaqeem selects 94 features globally. These features are ranked by Random Forest importance across all 16 classes, which is dominated by the majority class. CDS-OVR selects the 18 most discriminative features for each class independently — 109 unique features across 8 models, with pairwise Jaccard overlap as low as 0.023 (`evidence_analysis.py`, Section 4). Features like class 10's 13 unique features would never be ranked highly by global importance but are critical for RBBB detection.

2. **Per-class against_scale vs. uniform SVM**: The experiment above shows 17/78 OAO pairs have >10:1 imbalance, with the worst at 122.5:1. CDS-OVR's against_scale = 0.5 for rare classes halves the impact of negative evidence, compensating for the inherent imbalance. The ablation (Section 4.6) confirms this gives +9.2 pp for class 5 and +6.7 pp for class 4.

#### 5.2.2 Where Mustaqeem Has Structural Advantages

**Feature interactions via polynomial kernel**: SVM's polynomial kernel of degree d computes all d-way feature products, capturing conjunctive patterns that CDS-OVR's independent feature evaluation cannot detect. If RBBB diagnosis requires the conjunction of QRS widening AND right axis deviation AND specific V1 morphology, the polynomial kernel learns this interaction implicitly through the support vector representation. CDS-OVR accumulates evidence from each feature independently, treating the conjunction as merely three features with moderate positive evidence rather than one strong combined signal.

**Data augmentation**: Mustaqeem's 5× shadow augmentation (452 → 2,260) increases the effective training size for each pairwise classifier, reducing variance in the support vector estimates and stabilizing the hyperplane positions. CDS-OVR works with the original patient count.

### 5.3 Irfan et al. 2022 — CNN-LSTM

**Published in**: Sensors, 22(15), 5606 (2022)

**Method for UCI (Dataset D1)**:
1. Mean imputation for missing values
2. Discard features with >30% missing values
3. StandardScaler normalization
4. PCA → 50 principal components
5. 60/40 split → 271 train / 181 test
6. CNN (3 conv layers) + LSTM architecture, SoftMax output (13 classes)
7. 500 epochs, Adam optimizer, categorical cross-entropy

**Result**: 93.33% accuracy on UCI D1 (12 errors out of 181 test patients)

#### 5.3.1 Per-Class Comparison Using Irfan's Confusion Matrix

Irfan's published confusion matrix (their Table 11) enables class-level analysis:

| Class | n_test (Irfan) | Irfan Accuracy | CDS-OVR 10-fold CV (seed=13) | Structural Reason for Difference |
|-------|---------------|---------------|------------------------------|--------------------------------|
| 1 (Normal) | 96 | 97.9% | 91.4% (224/245) | Irfan: PCA preserves healthy cluster; CDS-OVR: healthy bar protects most but 21/245 flagged |
| 2 (IC/CAD) | 14 | 92.9% | 77.3% (34/44) | Both adequate with data; CDS-OVR's 10 misses go to class 1 (8) or others (2) |
| 3 (AM) | 7 | 71.4% | 93.3% (14/15) | CDS-OVR outperforms: per-class FS (feature 111 FDR=19.47) + 100% male class enables sex branching |
| 6 (SB) | 12 | 75.0% | 84.0% (21/25) | Irfan misclassifies 2 SB->AM; PCA conflates features. CDS-OVR uses FDR-weighted features per class |
| 10 (RBBB) | 24 | 91.7% | 82.0% (41/50) | Irfan's PCA preserves QRS morphology better; CDS-OVR loses 8 RBBB to class 1 (healthy bar overrides) |

*CDS-OVR per-class data from `evidence_analysis.py`, Section 14. Note: Irfan uses 60/40 split while CDS-OVR numbers above are 10-fold CV — different protocols.*

> **Experiment: Per-Class Accuracy at 60/40 (Protocol-Matched to Irfan)** (`evidence_ablation.py`, Section 3)
>
> **What was tested**: CDS-OVR per-class accuracy at 60/40 split across 10 seeds, providing protocol-matched comparison data for Irfan's 60/40 results.
>
> **Why**: The table above uses 10-fold CV data (CDS-OVR) vs. 60/40 split data (Irfan) — different protocols. To fairly compare, CDS-OVR per-class accuracy must be evaluated on the same 60/40 protocol.
>
> **Method**: For each of 10 seeds, a 60/40 random split is created (250 train / 167 test). CDS-OVR is trained and predictions are made for the 167 test patients. Per-class accuracy (correct/total for that class in the test set) is averaged across all 10 seeds.
>
> **Results** (mean per-class accuracy across 10 seeds at 60/40):
>
> | Class | CDS-OVR 60/40 Mean | CDS-OVR 60/40 Best | Irfan 60/40 | Δ (CDS mean vs. Irfan) |
> |-------|-------------------|-------------------|-------------|----------------------|
> | 1 (Normal) | 92.6% (σ=2.6%) | 98.0% | 97.9% | −5.3 pp |
> | 2 (CAD) | 63.6% (σ=8.3%) | 82.4% | 92.9% | −29.3 pp |
> | 3 (AM) | 97.6% (σ=5.0%) | 100.0% | 71.4% | **+26.2 pp** |
> | 6 (SB) | 56.4% (σ=18.6%) | 87.5% | 75.0% | −18.6 pp |
> | 10 (RBBB) | 64.1% (σ=8.6%) | 77.3% | 91.7% | −27.6 pp |
>
> **Interpretation**: At 60/40, CDS-OVR dramatically outperforms Irfan on class 3 (+26.2 pp) due to per-class feature selection (feature 111, FDR=19.47) and the 100% male composition enabling sex branching. However, CDS-OVR significantly underperforms on classes 2 and 10, which have sufficient data for Irfan's PCA+CNN-LSTM but require feature interaction patterns that CDS-OVR cannot capture (Section 7.1). The high variance (σ=8–19%) for smaller classes confirms that single-split comparisons at 60/40 are unreliable — Irfan's single-split 93.33% may or may not represent expected performance. CDS-OVR's class 6 performance is particularly unstable (range: 25–87.5%) because 10 test patients per seed is too few for stable estimates.

**Critical observation on class 6 (from Irfan's Table 11)**: Irfan misclassifies 2 sinus bradycardia patients as anterior MI (SB → AM). Clinically, these conditions have entirely different ECG signatures — bradycardia is a rate disorder (heart rate < 60 bpm), while anterior MI shows Q-wave and ST-segment changes. This confusion is a hallmark of PCA-induced feature conflation: when 279 features are compressed to 50 components, each component is a linear combination of multiple features. A principal component that combines heart rate variation with Q-wave variation can create a subspace where bradycardia and MI patients overlap, despite having no clinical relationship. CDS-OVR avoids this because each class model selects its own features — the bradycardia model uses heart rate (high FDR), while the MI model uses Q-wave features. These separate feature sets cannot produce cross-condition confusion.

**Additional observation on class L (LBBB)**: Irfan's confusion matrix shows class L with only 2 test instances, of which 1 is correctly classified and 1 is misclassified. The 50% sensitivity for LBBB highlights the fragility of evaluating on micro-classes: a single misclassification cuts sensitivity in half. This reinforces why CDS-OVR removes classes with insufficient representation (original UCI classes 7, 8, 14, 15, 16 with 3, 2, 4, 5, 22 patients respectively) — not to inflate accuracy, but because the smallest classes produce statistically meaningless evaluation metrics, and class 16 ("Other/Unclassified") lacks a coherent clinical definition for supervised learning.
=======
**Critical observation on class 6**: Irfan misclassifies 2 sinus bradycardia patients as anterior MI (SB → AM). Clinically, these conditions have entirely different ECG signatures — bradycardia is a rate disorder (heart rate < 60 bpm), while anterior MI shows Q-wave and ST-segment changes. This confusion likely arises from PCA: when 279 features are compressed to 50 components, each component is a linear combination of multiple features. A principal component that combines heart rate variation with Q-wave variation can create a subspace where bradycardia and MI patients overlap. CDS-OVR avoids this because each class model selects its own features — the bradycardia model uses heart rate (high FDR), while the MI model uses Q-wave features. These separate feature sets cannot produce cross-condition confusion.
>>>>>>> Stashed changes

#### 5.3.2 Why Irfan Achieves 93.33% on 60/40 While CDS-OVR Achieves 80.2% Mean

This gap deserves detailed analysis, though it is smaller than the raw numbers suggest due to class count differences (Irfan: 13 classes, CDS-OVR: 8 classes).

**Root cause: posterior quality degradation in CDS-OVR at 60% training**. CDS-OVR's evidence accumulation requires bin_count ≥ 3 at prediction time (hardcoded at `_compute_af()`, line 425). While bin availability at 60/40 is actually reasonable (87.5%+ for all classes, `evidence_analysis.py`, Section 6), the issue is posterior quality. With only 4 class-9 patients (60% of 9) spread across 18 features × 4.4 avg bins ≈ 79 bin slots, almost every bin has 0 target patients, producing posteriors near the prior (2.2%) with no discriminative signal.

Irfan's PCA + CNN-LSTM does not have this failure mode because:
1. PCA computes the covariance matrix from all 271 training patients regardless of class, so even rare classes contribute to the transformation.
2. The CNN-LSTM operates in the 50-dimensional PCA space, where even rare classes can form identifiable clusters (at the cost of potential overfitting with 500 epochs of training).

**Important caveat**: Irfan reports a single 60/40 split with no variance. CDS-OVR's 10-seed evaluation shows multiclass accuracy ranging from 75.4% to 86.2% at 60/40 (σ = 3.06%). If Irfan's 93.33% also has comparable variance, their expected performance might be closer to 85–90%. The gap is structurally real but the magnitude is uncertain.

### 5.4 Islam et al. 2023 — SVM + RF Serial Hybrid

**Published in**: arXiv:2301.10174 (2023 preprint)

**Method**: Remove columns with >50% missing values (279 → 252). Apply mRMR feature selection. Test SVM (71.4%), RF (72.3%), and SVM+RF serial hybrid (77.4%).

**CDS-OVR**: 84.7% mean 10-fold CV, 86.8% best

**Why CDS-OVR outperforms by 7–10 pp**: Four structural factors compound:

> **Experiment: Diagnostic Signal in Gupta's Deleted Features** (`evidence_analysis.py`, Section 12)
>
> **What was tested**: For the 4 features with missing values in the CDS-OVR dataset (features 10, 11, 12, 13), the Fisher discriminant ratio was computed per class to quantify how much diagnostic signal Gupta's column-deletion discards.
>
> **Why**: If deleted features have low FDR for all classes, deletion is harmless. If any deleted feature has high FDR for specific diseases, deletion removes signal that could improve rare-class detection.
>
> **Method**: For each of the 4 features with missing values, FDR = (μ_target − μ_rest)² / (σ²_target + σ²_rest) was computed for all 8 classes using available (non-NaN) values. Additionally, CDS-OVR's retained feature sets were checked to identify which class models actively use these features.
>
> **Results**:
> - Feature 13 (83.9% missing): FDR = **1.437 for class 3** (Old Anterior MI) — ranking #19 of 279 features for that class
> - Feature 12 (0.2% missing): FDR = 0.274 for class 5 (Sinus Tachycardia)
> - CDS-OVR class models using these features: class 1 uses features 10, 12; class 2 uses features 10, 12, 13; class 4 uses feature 12; class 5 uses features 11, 12
> - Total: **5 of 8 class models actively use at least one feature that Gupta deletes**
>
> **Interpretation**: Gupta's column deletion removes features actively used by 5 of 8 CDS-OVR class models. Feature 13's FDR of 1.437 for class 3 means it contributes meaningful evidence for Anterior MI detection despite being 83.9% missing — the 67 patients with valid values are sufficient for CDS-OVR to extract signal. Gupta's approach discards this signal entirely because even one missing value in a column triggers complete removal.

1. **Column deletion removes discriminative features.** Gupta et al. discard all columns containing any missing value (279 → 252 per their report, 27 features removed). The experiment above shows that deleted features carry diagnostic signal: feature 13 has FDR = 1.437 for class 3, and 5 of 8 CDS-OVR class models actively use at least one feature that Gupta deletes. In our 416-patient subset, 4 features have missing values (features 10, 11, 12, 13); Gupta's use of all 452 patients likely exposes missing values in additional features, accounting for the higher removal count (27 vs. 4). CDS-OVR retains all features and skips missing values at prediction time (`if np.isnan(v): continue`).

2. **Global mRMR dilutes rare-class feature relevance.** mRMR's mutual information criterion is dominated by majority-class separation. Features uniquely discriminative for rare classes (4, 5, 9) have low mutual information with the overall class label and are deprioritized. The fact that Gupta et al.'s SVM misclassified class 5 as class 1 directly confirms this: the features retained by mRMR did not include the features needed to separate class 5 (sinus tachycardia, 13 patients) from class 1 (normal, 245 patients). CDS-OVR's per-class feature selection ensures that each class gets its own 18 most discriminative features.

3. **No population stratification.** Sex-dependent ECG distributions (4 features with Cohen's d > 0.5, `evidence_analysis.py`, Section 8) and class 3 being 100% male mean that combined-sex models must learn more complex decision boundaries. CDS-OVR's sex branching (`cds_ovr.py`, line 135) creates more homogeneous subpopulations.

> **Ablation Experiment: sex branching effect** (`evidence_ablation.py`, Section 2)
>
> **What was tested**: CDS-OVR with sex branching enabled (SEX_FEAT=1, current design) vs. disabled (SEX_FEAT=-1, no population splitting by sex).
>
> **Why**: To quantify how much accuracy sex-based stratification contributes, isolating it from other CDS-OVR mechanisms.
>
> **Method**: Both configurations evaluated via 10-fold CV across 10 seeds. When disabled, all patients are processed through a single branch rather than separate male/female branches for feature selection and binning.
>
> **Results**: With sex branching: 84.66% mean (σ=0.93%). Without: 84.25% mean (σ=0.88%). Difference: **+0.41 pp**, wins in 6/10 seeds.
>
> **Interpretation**: Sex branching provides a modest but consistent improvement (+0.41 pp). The effect is concentrated in class 3 (Old Anterior MI), which is 100% male — sex branching gives it a dedicated male-only feature set, avoiding dilution by female patients. The overall accuracy impact is small because most classes have both sexes represented. The fact that Gupta et al. (and all other benchmarks) do not stratify by sex means they face marginally more complex decision boundaries, though the quantified effect suggests this is a secondary factor in the accuracy gap.

4. **SVM+RF serial stacking amplifies noise (structural argument).** The second-stage RF receives SVM predictions as input features, but SVM achieves only 70% accuracy (Gupta et al.'s reported SVM-mRMR result). With a 30% error rate, the RF's input contains systematic misclassification patterns on a small dataset. The risk is that the RF learns to compensate for specific SVM errors in the training set rather than learning generalizable class structure — the marginal improvement from 70% (SVM alone) to 77.4% (SVM+RF hybrid) is consistent with partial error correction rather than deep complementarity.
=======
4. **SVM+RF serial stacking amplifies noise.** The second-stage RF receives SVM predictions as input features, but SVM achieves only 71.4% accuracy — a 28.6% error rate. The RF must learn from these noisy pseudo-features on a small dataset, likely overfitting to the SVM's error patterns.
>>>>>>> Stashed changes

### 5.5 Hybrid FS 2021 — Three-Stage Feature Selection with Ensemble Voting

**Published in**: IEEE CCECE 2021 (Khan Mamun)

**Method**: Pearson r > 0.05 → 71 features; MRMR MIQ → 40 features; RFE with RF → 32 features. Binary classification with 8-classifier ensemble (LR, DT, SVM, LDA, QDA, NB, RF, KNN). Hard/soft voting.

**Result**: 77.27% accuracy (binary)

**CDS-OVR binary**: 97.6% best, 88.6% mean (both 90/10)

**Why the 11–20 pp gap**: The gap traces to three interacting factors:

> **Experiment: Feature Coverage Analysis — 32 Global Features vs. Per-Class Selection** (`evidence_analysis.py`, Section 4)
>
> **What was tested**: Whether a 32-feature global selection (Hybrid FS's final count) can adequately cover the per-class feature requirements identified by CDS-OVR's 8 independent class models.
>
> **Why**: To quantify the information loss from using a single global feature set for all diseases. If different diseases require genuinely different features, then any fixed-size global set must sacrifice coverage for some diseases.
>
> **Method**: CDS-OVR's 8 class models (trained on full data, 10-fold CV seed=13) were examined to extract: (a) total unique features across all models, (b) features unique to each class model (not shared with any other), (c) pairwise Jaccard similarity between class feature sets.
>
> **Results**:
> - Total unique features required across all 8 models: **109**
> - Features unique to a single class: class 10 = **13 unique features** (QRS morphology), class 4 = 10 unique, class 3 = **8 unique features** (Q-wave indicators, incl. feature 111 with FDR=19.471), class 2 = 7 unique
> - Minimum pairwise Jaccard overlap: **0.023** (class 3 vs. class 10 — virtually no feature sharing)
> - Maximum pairwise Jaccard overlap: 0.289 (class 4 vs. class 6)
> - A 32-feature global set can cover at most 32/109 = **29.4%** of the feature requirements. It must omit at least 77 features that are discriminative for specific diseases.
>
> **Interpretation**: With 109 features needed across 8 diseases and pairwise overlap as low as 2.3%, a 32-feature global set is structurally insufficient. Class 10 alone needs 13 unique features not shared with any other class — if even half of these are excluded from the global 32, RBBB detection is severely compromised. Similarly, class 3's feature 111 (FDR=19.471) is the single most discriminative feature in the entire dataset for any class, but it ranks low in global importance because class 3 has only 15 patients (3.6% of the dataset). Any global feature selection method (Pearson, mRMR, RFE) that ranks by aggregate importance will deprioritize it.

1. **32 global features cannot cover all diseases.** The experiment above quantifies the problem: CDS-OVR requires 109 unique features for adequate per-class discrimination, and pairwise Jaccard overlap as low as 0.023 proves that different diseases need genuinely different features. A 32-feature global set covers at most 29.4% of requirements.

2. **Ensemble voting with diverse but unequal classifiers.** The ensemble's accuracy is bounded by the majority behavior of its 8 classifiers. In high-dimensional ECG data with 32 features, linear classifiers (LDA, QDA) and distance-based methods (KNN) are structurally limited — they cannot learn per-class decision boundaries from a 32-feature global set. The ensemble's final vote averages these limited classifiers with stronger ones (RF, SVM), pulling the overall accuracy toward the median rather than the maximum. CDS-OVR does not ensemble classifiers — it accumulates evidence from features within a single coherent framework.

3. **No adaptive thresholds.** A single binary threshold must balance sensitivity and specificity across all disease classes simultaneously. CDS-OVR's per-class CLASS_THRESHOLDS and healthy bar provide disease-specific sensitivity/specificity tradeoffs.

### 5.6 Jadhav et al. 2012 — Artificial Neural Networks

**Published in**: Published 2012 (exact venue in reference list of other papers)

**Method**: Replace missing values with closest column value from same class. Train MLP (1–3 hidden layers), GFFNN, MNN. Binary classification. Multiple splits.

**Key result** (85/15 split): MLP: 86.67% accuracy, 93.75% sensitivity, 78.57% specificity

**Analysis of the sensitivity/specificity imbalance**:

Jadhav's MLP achieves high sensitivity (93.75% — catches most diseased patients) but low specificity (78.57% — 21.4% of healthy patients are false positives). This imbalance reveals a structural problem: the MLP uses all 279 features without feature selection, and in high-dimensional space, healthy patients are more likely to be pushed across the decision boundary by noisy features than diseased patients are to be pulled back.

**Direct split-matched comparison** (CDS-OVR binary accuracy at 90/10 from `run_all_splits.py`):

| Metric | Jadhav MLP 2HL (DS5) | CDS-OVR Best (90/10) | CDS-OVR Mean (90/10) |
|--------|---------------------|---------------------|---------------------|
| Accuracy | 86.67% | 97.6% | 88.6% |
| Sensitivity | 93.75% | 93.3% (best seed) | — |
| Specificity | 82.76% | 100.0% (best seed) | — |

> **Experiment: Full Sensitivity/Specificity Distribution at 90/10** (`evidence_ablation.py`, Section 5)
>
> **What was tested**: CDS-OVR's binary sensitivity and specificity were computed for all 10 seeds at 90/10 split, providing a distribution rather than a single point estimate.
>
> **Why**: Jadhav reports single sensitivity/specificity numbers. To fairly compare, CDS-OVR needs its full distribution — showing whether its specificity advantage is consistent or seed-dependent.
>
> **Method**: For each of 10 seeds, the 90/10 split is created, CDS-OVR is trained and tested on the 42 test patients. Binary TP/FN/TN/FP are computed (disease patient predicted as any disease = TP; disease patient predicted healthy = FN; healthy patient predicted healthy = TN; healthy patient predicted diseased = FP).
>
> **Results** (all 10 seeds):
>
> | Seed | Binary Acc | Sensitivity | Specificity | TP | FN | TN | FP |
> |------|-----------|------------|------------|----|----|----|----|
> | 13 | 85.7% | 83.3% | 88.9% | 20 | 4 | 16 | 2 |
> | 20 | 85.7% | 78.9% | 91.3% | 15 | 4 | 21 | 2 |
> | 27 | 81.0% | 73.3% | 85.2% | 11 | 4 | 23 | 4 |
> | 34 | 90.5% | 80.0% | 96.3% | 12 | 3 | 26 | 1 |
> | 41 | 88.1% | 72.2% | 100.0% | 13 | 5 | 24 | 0 |
> | 48 | 92.9% | 84.2% | 100.0% | 16 | 3 | 23 | 0 |
> | 55 | 88.1% | 71.4% | 96.4% | 10 | 4 | 27 | 1 |
> | 62 | 92.9% | 88.2% | 96.0% | 15 | 2 | 24 | 1 |
> | 69 | 83.3% | 78.9% | 87.0% | 15 | 4 | 20 | 3 |
> | **76** | **97.6%** | **93.3%** | **100.0%** | **14** | **1** | **27** | **0** |
>
> **Mean sensitivity**: 80.4% (σ=7.0%). **Mean specificity**: 94.1% (σ=5.5%).
>
> **Interpretation**: CDS-OVR achieves **higher mean specificity** (94.1%) than Jadhav's MLP (82.76%), meaning fewer healthy patients are incorrectly flagged. Three seeds achieve 100% specificity (FP=0). However, CDS-OVR's **mean sensitivity** (80.4%) is lower than Jadhav's (93.75%), meaning it misses more disease patients on average. The best seed (76) achieves 93.3% sensitivity and 100% specificity simultaneously — matching Jadhav's sensitivity while far exceeding specificity. The tradeoff: CDS-OVR's healthy bar deliberately favors specificity over sensitivity (preventing false alarms at the cost of some missed diseases), while Jadhav's MLP has no such mechanism and over-triggers on healthy patients.

CDS-OVR achieves more balanced sensitivity/specificity through:
- **Feature selection** (18 per class): eliminates noisy features that produce false positives
- **Healthy bar**: raises disease thresholds when healthy evidence is strong, preventing false positives (91.4% specificity in 10-fold CV, `evidence_analysis.py`, Section 13)
- **Per-class thresholds**: tunes the sensitivity/specificity tradeoff independently for each disease

Jadhav's MNN achieves the opposite pattern: 77.78% sensitivity, 93.10% specificity. The MNN's modular architecture is conceptually similar to CDS-OVR's per-class models, and its high specificity suggests that modular decomposition helps identify healthy patients. But MNN's lower sensitivity indicates that the sub-networks cannot reliably detect diseases from their respective feature subsets without the sophisticated evidence accumulation and Fisher weighting that CDS-OVR provides.

---

## 6. The 60/40 Performance Gap: A Structural Analysis

CDS-OVR's weakest result (60/40: 80.2% mean multiclass, 86.2% best) contrasts with Irfan's 93.33% on the same protocol. However, CDS-OVR's 60/40 binary accuracy tells a different story: 92.2% best, 84.5% mean. The gap narrows significantly when comparing the same task type:

| Metric | Irfan CNN-LSTM (13-class) | CDS-OVR Best (8-class multi) | CDS-OVR Mean (8-class multi) | CDS-OVR Best (binary) |
|--------|--------------------------|-----------------------------|-----------------------------|----------------------|
| 60/40 Accuracy | 93.33% | 86.2% | 80.2% | 92.2% |

This section analyzes the structural causes of the multiclass gap in detail.

### 6.1 The Minimum Evidence Density Problem

CDS-OVR's classification pipeline has a hard floor: `if bc < 3: continue` in `_compute_af()`. This means that for any feature-bin pair with fewer than 3 patients in the bin during training, zero evidence is accumulated during prediction.

> **Experiment: Bin Density and Posterior Quality Across Split Ratios** (`evidence_analysis.py`, Section 6)
>
> **What was tested**: For each class model at 60/40 and 90/10 splits, the proportion of feature-bin pairs that have ≥3 patients (usable bins) was measured, along with the expected number of target-class patients per bin.
>
> **Why**: To determine whether CDS-OVR's 60/40 performance deficit is caused by (A) bins being too empty to pass the `bc ≥ 3` filter, or (B) bins passing the filter but containing too few target-class patients to produce discriminative posteriors. Distinguishing these causes identifies whether the fix is algorithmic (lower the filter threshold) or architectural (the model fundamentally needs more per-class data).
>
> **Method**: At each split ratio, for each class model, training data was binned using CDS-OVR's supervised chi-squared procedure. For each feature-bin pair: total bin count (bc) and target-class count within that bin were recorded. A bin is "usable" if bc ≥ 3, and "discriminative" if it contains ≥1 target-class patient (producing a posterior shift above the prior).
>
> **Results**: See tables below. Key finding: bin *availability* at 60/40 is adequate (87.5%+ for all classes), but target-class *density* is critically low for rare classes. Class 9 (LBBB) at 60/40 has only 4 training patients spread across ~79 bin-feature slots → average 0.05 target patients per bin → posteriors ≈ prior (2.2%) with no discriminative power.
>
> **Interpretation**: The problem is not bin availability but **posterior quality**. Lowering the `bc ≥ 3` threshold would not help because the bins already pass the filter — they just contain no target-class patients. The fundamental issue is that 4 patients cannot populate enough bins to create discriminative posterior shifts across 18 features. This is a data-per-class problem, not a threshold problem.

**Bin density across split ratios** (`evidence_analysis.py`, Section 6, seed=13):

At 60% training:

| Class | n_train_60% | Bins with ≥3 patients | Effective model capacity |
|-------|------------|----------------------|------------------------|
| 1 (Normal) | 153 | 6/6 (100%) | Full |
| 2 (CAD) | 22 | 6/6 (100%) | Adequate |
| 10 (RBBB) | 28 | 6/6 (100%) | Adequate |
| 6 (SB) | 15 | 6/6 (100%) | Adequate |
| 4 (IM) | 8 | 5.8/6 (92.4%) | Adequate |
| 3 (AM) | 11 | 5.4/6 (100%) | Adequate |
| 5 (ST) | 8 | 6/6 (91.7%) | Marginal |
| 9 (LBBB) | 4 | 4.4/6 (87.5%) | Limited |

*Note: Bin density values from evidence_analysis.py using seed=13. "Bins with ≥3" shows usable bins / total bins.*

The bin density data (from `evidence_analysis.py`, Section 6, seed=13) reveals that bin *availability* at 60/40 is actually reasonable: even class 9 (LBBB, 4 training patients) has 87.5% usable bins, and most classes exceed 90%. The performance deficit at 60/40 is therefore not primarily a bin-count problem but a **posterior quality** problem: with only 4 class-9 patients spread across 18 features × ~4.4 avg bins ≈ 79 bin-feature slots, the vast majority of bins contain 0 target patients, yielding posteriors near the prior (2.2%) for almost every bin. The model technically "works" — bins exist and pass the `bc ≥ 3` filter because the ~412 non-target patients provide sufficient bin counts — but the posterior estimates carry almost no discriminative signal.

At 90/10, the picture improves substantially:

| Class | n_train_90% | Avg bins | Usable bins (bc≥3) | Usable% |
|-------|------------|----------|-------------------|---------|
| 1 (Normal) | 227 | 7.0 | 216/216 | 100.0% |
| 2 (CAD) | 33 | 7.7 | 216/216 | 100.0% |
| 10 (RBBB) | 44 | 8.5 | 205/205 | 100.0% |
| 6 (SB) | 23 | 6.5 | 213/213 | 100.0% |
| 4 (IM) | 12 | 6.6 | 180/204 | 88.2% |
| 3 (AM) | 14 | 5.5 | 99/99 | 100.0% |
| 5 (ST) | 13 | 8.0 | 185/215 | 86.0% |
| 9 (LBBB) | 8 | 5.6 | 125/141 | 88.7% |

With 8 class-9 patients at 90/10 (vs. 4 at 60/40), the average bin count rises from 4.4 to 5.6, and more importantly, the 8 target patients can populate ~2–3 bins with non-trivial posterior shifts, producing meaningfully differentiated evidence signals.

### 6.2 Why PCA-Based Methods Do Not Have This Problem

PCA computes the covariance matrix from all training patients regardless of class. With 271 training patients at 60/40, the covariance matrix is well-estimated (271 >> 50 components). Every class — including class 9 with only 4 training patients at 60/40 — contributes to and is represented in the PCA transformation. The CNN-LSTM then operates in the 50-dimensional PCA space, where even rare classes can form identifiable clusters.

This is the fundamental tradeoff: **CDS-OVR's class-aware specialization requires sufficient per-class data; PCA-based methods share representation learning across all classes but cannot specialize per-class.**

### 6.3 Potential Resolutions

Several modifications could improve CDS-OVR's 60/40 performance without compromising its 90/10 advantage:

1. **Adaptive bin count**: Reduce MAX_BINS when per-class training data is small (e.g., MAX_BINS = max(2, min(6, n_class_train / 3))).
2. **Lower prediction MIN_SUPPORT for rare classes**: Allow `bc ≥ 2` for rare classes during prediction, accepting higher posterior variance.
3. **Feature-level fallback**: When retained actions are too few, use the single highest-FDR feature with a simple threshold rule instead of binning.

---

## 7. Honest Assessment: Structural Advantages of Benchmark Methods

### 7.1 Feature Interactions (All Neural/Kernel Methods)

CDS-OVR evaluates features independently. The evidence from feature A is added to the evidence from feature B without considering their joint distribution. This means CDS-OVR cannot detect conjunctive patterns where multiple features must simultaneously meet criteria for a diagnosis. SVM (polynomial kernel), CNN, and RF (tree splits) all capture such interactions through their respective mathematical formulations.

> **Experiment: Error Pattern Analysis Revealing Feature-Interaction Deficit** (`evidence_analysis.py`, Section 14)
>
> **What was tested**: The confusion matrix from 10-fold CV (seed=13, all 416 patients tested) was analyzed to identify systematic error patterns — specifically, whether misclassifications cluster in ways that suggest multi-feature conjunctive patterns are needed.
>
> **Why**: If CDS-OVR's errors are random (evenly distributed across classes), the independent-feature assumption is not the bottleneck. If errors cluster in specific class→class transitions, it suggests those transitions require feature interactions that CDS-OVR cannot capture.
>
> **Method**: After 10-fold CV (seed=13), each of the 55 misclassified patients was categorized by true class → predicted class. The most frequent error transitions were identified and analyzed for clinical correlation with known multi-feature diagnostic criteria.
>
> **Results**:
> - Total errors: 55/416 (13.2% error rate)
> - **Class 10 (RBBB) → Class 1 (Normal): 8 errors** (14.5% of all errors, 16% of class 10's 50 patients)
> - Class 2 (CAD) → Class 1 (Normal): 10 errors (22.7% of class 2's 44 patients)
> - Class 5 (ST) → Class 1 (Normal): 5 errors (38.5% of class 5's 13 patients)
>
> **Interpretation**: The 8 RBBB→Normal errors are the most structurally informative. RBBB diagnosis requires the simultaneous conjunction of: QRS duration > 120 ms AND right axis deviation AND rSR' pattern in V1 AND wide S-wave in leads I/V6. CDS-OVR evaluates each feature independently — a patient with borderline QRS (118 ms, just below threshold), moderate right axis (85°), and mild V1 changes may receive three moderate positive signals that sum to an insufficient ratio score. A method that captures the conjunction (SVM polynomial kernel, RF tree path) would evaluate "QRS near 120 AND axis rightward AND V1 rSR'" as one strong combined pattern rather than three weak independent signals. This is likely the primary structural ceiling keeping CDS-OVR's 10-fold CV accuracy near 84–87% rather than exceeding 90%.

### 7.2 Continuous vs. Discrete Decision Boundaries

CDS-OVR's bins create step-function posteriors. Within a bin, all patients receive the same p_class estimate regardless of their exact feature values. A patient at QRS = 119.9 ms and another at QRS = 120.1 ms may fall in different bins and receive different evidence contributions, despite near-identical clinical presentations.

SVM and neural network decision boundaries are continuous functions of the input features, providing smoother and potentially more accurate classifications near decision boundaries.

### 7.3 End-to-End Optimization

CDS-OVR's staged pipeline (binning → posterior estimation → feature selection → threshold setting) optimizes each stage with a local objective. The chi-squared binning does not know the downstream effect of its split points on the ratio score. Neural networks and gradient-boosted trees optimize a single loss function end-to-end, producing globally coherent models.

The tradeoff: end-to-end optimization requires more data to avoid overfitting. CDS-OVR's staged optimization is more sample-efficient, which explains its strong performance at 90/10 where most classes have ≥10 training patients but the total dataset is too small for deep learning to fully generalize.

---

## 8. Summary of CDS-OVR vs. Benchmark Performance Sources

### Why CDS-OVR Outperforms When It Does (90/10, 10-fold CV)

| CDS-OVR Mechanism | Effect | Numerical Evidence | Benchmark Lacking This |
|-------------------|--------|-------------------|----------------------|
| Per-class feature selection (18 features via FDR) | Each disease gets its most discriminative features; 109 unique features across 8 models | Jaccard overlap 0.023–0.289; class 10 has 13 unique features; class 3 has 8 unique features (`evidence_analysis.py`, §4) | All benchmarks use global feature sets |
| Per-class imbalance adaptation (against_scale, MIN_SUPPORT) | Rare classes detected from sparse evidence; reduced penalty for negative evidence | Ablation: against_scale=0.5 gives +9.2 pp for class 5, +6.7 pp for class 4 vs. uniform 0.8 (`evidence_ablation.py`, §1); 17/78 OAO pairs have >10:1 imbalance (`evidence_analysis.py`, §1) | All benchmarks use uniform parameters |
| Dual AF with ratio scoring | Separates for/against evidence; graduated confidence | Correct: mean ratio=6.86, median=4.55; Wrong: mean ratio=1.66, median=1.25 (`evidence_analysis.py`, §10) | Original CDS, most benchmarks use single decision function |
| Dynamic healthy bar + suspicion | Adaptive sensitivity/specificity per patient context | 91.4% specificity, 84.8% disease detection, 8.6% FPR (`evidence_analysis.py`, §13) | No benchmark adapts thresholds based on healthy evidence |
| Supervised chi-squared binning | Bin boundaries align with class boundaries | Class 6: supervised 2.5× stronger posterior shifts; class 6 best bin posterior=0.754 vs prior=6.0% (`evidence_analysis.py`, §9) | Original CDS uses unsupervised bins |
| Correlation filtering (|r| < 0.8) | Non-redundant feature sets maximize information per slot | Sharma retains 32 pairs with |r|>0.8; CDS-OVR enforces threshold per branch (`evidence_analysis.py`, §3, §11) | Sharma retains correlated features; no benchmark filters per-class |
| Sex-based population branching | More homogeneous subpopulations; class 3 is 100% male | Ablation: sex branching +0.41 pp overall (84.66% vs 84.25%), wins 6/10 seeds; 4 features with Cohen's d>0.5 (`evidence_ablation.py`, §2; `evidence_analysis.py`, §8) | No benchmark stratifies by sex |

### Why CDS-OVR Underperforms When It Does (60/40)

| Limitation | Effect | Numerical Evidence | Benchmark Advantage |
|-----------|--------|-------------------|-------------------|
| Posterior quality degradation | With 4 class-9 patients across ~79 bin slots at 60/40, posteriors near prior (2.2%) with no discriminative signal | Class 9 at 60/40: 4 train patients, 87.5% usable bins but posteriors ~= prior; at 90/10: 8 patients, posteriors diverge (`evidence_analysis.py`, §6) | PCA-based methods share representation across all classes |
| Independent per-class models | No parameter sharing; class 9 model cannot borrow from class 1's 245 patients | 60/40 mean accuracy: 80.2% (σ=3.06%) vs Irfan's 93.33%; 90/10 mean: 86.2% — the gap closes with more per-class data (`run_all_splits.py`) | Neural networks and RF learn shared representations |
| No feature interactions | Cannot detect conjunctive diagnostic patterns (e.g., QRS width AND axis deviation for RBBB) | 8 RBBB→Normal misclassifications in 10-fold CV, likely requiring joint feature evaluation (`evidence_analysis.py`, §14) | SVM (polynomial kernel), CNN, RF capture interactions |
| Discrete bin boundaries | Step-function posteriors lose information near boundaries | MAX_BINS=6 creates coarse partitions; patients at boundary receive identical evidence regardless of exact value | SVM and NN have continuous decision functions |

### The Core Insight

CDS-OVR's class-aware specialization — the property that distinguishes it most fundamentally from all benchmarks — is simultaneously the source of its best performance and its primary limitation. When per-class data is sufficient (≥8–10 training patients per class, as at 90/10), specialization produces models tailored to each disease that outperform one-size-fits-all approaches. When per-class data falls below a critical threshold (~3 patients per bin), the specialized models become inoperative and the system loses its advantage.

This is not a flaw to be eliminated but a design tradeoff to be understood: CDS-OVR achieves its strong 90/10 and 10-fold CV results precisely because it specializes per-class, and that same specialization requires sufficient per-class data to produce meaningful posteriors. At 60/40, class 9 (LBBB) has only 4 training patients — the bins exist (87.5% usable) but the posteriors carry almost no signal. At 90/10, 8 training patients per class is sufficient for 2–3 bins to develop discriminative posteriors, enabling the model's structural advantages to activate.

### 8.1 Distinguishing Preprocessing Choices from Algorithmic Innovation

Many of CDS-OVR's advantages over benchmarks stem from *preprocessing decisions* (retaining all patients, keeping all features, stratifying by sex) rather than from the classification algorithm itself. These are important but should not be conflated with algorithmic superiority:

| Category | CDS-OVR Choice | What It Really Is |
|----------|----------------|-------------------|
| Missing data handling | Skip NaN per-feature at prediction time | **Preprocessing choice** — any classifier could adopt this |
| Sex branching | Split population by feature 1 | **Preprocessing choice** — any pipeline could add this step |
| Retaining all 279 features as candidates | No column deletion | **Preprocessing choice** — preserves signal but requires per-feature NaN handling |
| Per-class feature selection (18 via FDR) | Class-specific Fisher scoring + correlation filter | **Algorithmic innovation** — requires OVR decomposition; cannot be trivially added to global classifiers |
| Dual AF with ratio scoring | Separate for/against evidence channels | **Algorithmic innovation** — fundamentally different inference mechanism from SVM margins or softmax |
| Dynamic healthy bar + suspicion | Patient-specific threshold adaptation | **Algorithmic innovation** — integrates healthy evidence into disease thresholds |
| Per-class against_scale | Imbalance-aware evidence weighting | **Algorithmic innovation** — encodes asymmetric confidence in positive vs. negative evidence for rare classes |

The honest assessment: if Sharma's RF were given the same preprocessing (no row deletion, no column deletion, per-class feature selection, sex stratification), it would likely close much of the accuracy gap. CDS-OVR's genuine algorithmic contributions — dual AF, healthy bar, per-class thresholds — are most impactful for sensitivity/specificity balance and rare-class detection, which are the clinically relevant metrics rather than raw accuracy.

The benchmark methods' advantages — feature interaction capture (SVM kernel, RF splits, CNN), end-to-end optimization (neural networks), principled regularization (L1/L2 penalties), calibrated probability outputs (LR, GNB), built-in variance reduction (RF bagging) — represent decades of validated ML research. CDS-OVR is a purpose-built system for this specific problem structure; it succeeds where domain-specific design choices align with dataset properties, and it lacks the generalization guarantees of established frameworks.

# Documentation By Kian for why machine learning methods adopted by other papers fail in areas CDS accounts for:

# Paper 1: A comparative study of heterogeneous machine learning algorithms for arrhythmia classification using feature selection technique and multidimensional datasets

Where CDS is stronger
1. Genuine multi-class architecture vs. binary collapse. The paper never actually solves the 13-class problem — it binarizes before training. CDS's OVR decomposition trains an independent evidence accumulator per class and combines them via per-class thresholds, which is architecturally suited to resolving which arrhythmia subtype a patient has, not just whether they have one. This is the single biggest structural advantage, since it's the harder and more clinically useful task.
2. Per-class handling of severe class imbalance. As established, RARE_CLASSES gets its own support/confidence/against-scale parameters. None of LR/LDA/GNB/SVM/RF/XGB in the paper have any per-class accommodation — they fit one global decision rule and would default toward majority-class behavior on 2–4-instance classes.
3. Target-aware feature scoring per class. CDS's _train_ovr_node searches all 279 features separately for each class using a class-specific shift/confidence/Fisher score. The paper's Pearson-correlation filter selects features based on correlation with other features, not the label — it's not even target-aware, let alone class-aware.
4. Native handling of missing data. branch_match returns False on NaN, and binning simply skips missing values feature-by-feature. The paper drops an entire 80%-missing column and then drops every row with any remaining missing value, shrinking 452 patients to 420 before a single model is trained. CDS retains the full 452 and lets each feature's model use whatever data exists for that feature.
5. No leakage in fold isolation. build_tree/train are rebuilt from scratch on training-fold-only data each CV iteration. The paper's feature selection happens before the split (Figure 1), which is exactly the mistake Ch. 11 of your Zollanvari reading quantified.
6. No preprocessing dependency. CDS's bin-based scoring is invariant to monotonic transforms of each feature — no normalization/scaling step is needed (contrast with Figure 4's explicit before/after normalization in the paper), and no imputation strategy has to be chosen and justified.
Where the paper's ML algorithms could be stronger
1. Battle-tested implementations vs. bespoke code. LR/RF/XGB/SVM in scikit-learn have been validated across thousands of studies and datasets; bugs get found and fixed by a huge user base. CDS is a ~600-line custom research script with a documented history of real bugs in this exact codebase (the prior-conflation bug, the partner's best-of-N-seeds selection bias). The prior probability of an undiscovered bug in CDS is meaningfully higher than in sklearn.ensemble.RandomForestClassifier.
2. Principled, systematic hyperparameter search vs. hand-tuned constants. Grid Search over a defined space (Table 23) — even though it's undermined here by pre-split leakage — is at least a documented, reproducible procedure. CDS's CLASS_THRESHOLDS, MIN_SUPPORT_MAP, CONF_SUPPORT_MAP, AGAINST_SCALE_MAP are hardcoded constants with a changelog in the docstring ("class 10 threshold = 3.0, lowered from 3.5") that reads like manual iteration against eval numbers rather than a defined search procedure with a held-out validation layer.
3. Far fewer degrees of freedom to overfit. LR has 3 hyperparameters (penalty, C, solver). CDS has per-class support/confidence/scale/threshold values — effectively dozens of tunable knobs across 13 classes, all fit (or hand-picked) against a 452-patient dataset. More knobs relative to sample size is a direct overfitting risk, independent of whether each individual mechanism is well-motivated.
4. Built-in variance reduction via ensembling. This is the one that connects directly to the split-variance discussion: Random Forest's bagging explicitly averages over many bootstrap-resampled trees specifically to reduce variance from which patients happen to be sampled. CDS builds exactly one deterministic tree per training set (build_tree isn't randomized or resampled at all) — there's no internal mechanism analogous to bagging that would dampen the kind of small-sample split sensitivity you saw in the 88.6%/97.6% gap. If instability across folds turns out to be a real problem for CDS under LOOCV, RF's architecture has a structural answer for it that CDS's doesn't.
5. Calibrated/free uncertainty estimates. LR and GNB output genuine class probabilities; RF gets out-of-bag error estimates without a separate CV loop. CDS's for:against ratio score is not a calibrated probability, and there's no analogous "free" internal validation signal — every uncertainty estimate CDS needs (LOOCV, seed-variance) has to be computed externally and by hand.
6. Unified regularization objective. XGB and LR regularize via an explicit penalty term in a single loss function (L1/L2), which is theoretically grounded and has one clean knob to tune. CDS's "regularization" is distributed across several separate ad hoc mechanisms (min support cutoffs, confidence caps, against-scale down-weighting, threshold offsets) with no unifying objective — harder to reason about what any one of them is actually trading off against the others.
The throughline: CDS's advantages are almost all about being purpose-built for this exact problem's structure (rare classes, multi-subtype, missing data). The paper's methods' advantages are almost all about being general-purpose tools with decades of validation and fewer ways to quietly go wrong. Neither list resolves the open question from before — whether CDS's purpose-built advantages actually pay off once you strip away seed selection and pre-split leakage on both sides is still what the LOOCV run would tell you.

Kian's Note: A lot of the reasons listed boils down to how CDS preprocesses data a different way (by keeping the rare disease classes for example), and how the machine learning models utilize ensmeble learning, allowing for more generalization as well as being established techniques in the filed of cyber-physical systems. Not very good reasons presented by Claude.

# Paper 2: Classification of Arrhythmia Using Machine Learning Techniques

Where CDS is stronger
1. OneR's single-attribute ceiling is exactly the failure mode Holte's own paper predicts for this dataset. Holte's soybean example is the direct analogue: with 15 classes and no single attribute cleanly separating more than a handful of them, a 1-rule tops out around 86% even under ideal conditions — and that's the best case, assuming one attribute perfectly separates the largest classes. Holte's own diagnostic (his §2, "characteristic of the CH and SO datasets") is precisely "is there any attribute with more distinct values than there are classes?" For arrhythmia — 279 attributes, most either binary or supervised-binned into a handful of levels, against 13-16 classes with several at n<5 — that condition almost certainly fails. OneR's 56–61% in Table 1 is consistent with exactly this bound, not a training/implementation problem. CDS's OVR decomposition sidesteps this entirely: it never needs one attribute to do all the work, because each class gets its own multi-feature evidence accumulator (up to FEATURES_PER_CLASS=18 features).
2. J48's split criterion is majority-class-dominated by construction, which structurally starves rare classes. Witten & Frank are explicit: "a decision tree split takes all classes into account, trying to maximize the purity of the split" — information gain/gain ratio is computed by pooling all classes at a node. With Normal ECG at 245 and Right Bundle Branch Block at 50 dwarfing Supraventricular Premature Contraction at 2, the entropy reduction from any split is overwhelmingly driven by the big classes; there's little gain-based incentive to carve out a branch that isolates a 2-instance class. Compounding this, C4.5's default "minimum instances per branch outcome" is 2 (stated explicitly in the textbook) — meaning several of this dataset's classes sit right at the edge of the threshold below which a split is simply not created. Then postpruning compounds it again: the pessimistic error estimate is an upper confidence bound computed from training-set N and E, and small N produces wide confidence intervals and therefore high pessimistic error — which is exactly the condition that triggers pruning a subtree back to a leaf. So a rare class isn't just hard to split on; if it does get a branch, it's disproportionately likely to get pruned away afterward. This is also probably why the authors themselves describe J48's accuracy as "unstable" across splits (Table 1's 25-point range, 91.81% training-set-itself down to 67.03% at 80/20) while OneR and Naive Bayes are comparatively stable — J48's rare-class branches are exactly what's flickering in and out depending on which few instances land in which split.
CDS's answer: MIN_SUPPORT_MAP/CONF_SUPPORT_MAP explicitly lower the support bar for RARE_CLASSES rather than applying one global constant, and there's no pruning step that can eliminate a class's evidence accumulator entirely — every class always gets a shot regardless of how rare.
3. Naive Bayes's per-class Gaussian/frequency estimates get unstable exactly where it matters most. Langley et al.'s formal analysis is instructive here specifically because of what it doesn't model: their whole treatment assumes P(C)=½ (explicitly held constant "to normalize for effects of the base rate") and Boolean, independent, noise-free attributes. Their graceful-degradation result is real but it's derived under balanced classes — it says nothing about what happens when a class has 2–3 training instances, which is the actual regime several arrhythmia classes are in. With n=2, a per-class Gaussian variance estimate (which WEKA's Naive Bayes needs for continuous attributes) is close to degenerate. Interestingly, Witten & Frank's generalized Laplace/m-estimate — (count + m·p) / (N + m), where p is a prior probability the counts get shrunk toward — is the same formula as CDS's p_class = (target_counts + LAPLACE_ALPHA·prior) / (bin_counts + LAPLACE_ALPHA), just with p set to the node-local class prior rather than WEKA Naive Bayes's default uniform 1/k. That's a real point in CDS's favor: it's using the textbook's own "more principled" generalization of Laplace smoothing (which Witten & Frank note is harder to justify but more rigorous), while WEKA's default Naive Bayes uses the simpler add-1 version.
Where OneR, J48, and Naive Bayes could be stronger (or CDS's exposure the comparison reveals)
1. Missing-value handling is inconsistent across the three algorithms in this very paper, and the paper's own description glosses over it. Soman & Bobbie state blanket-style that "the instance with missing values is probabilistically assigned a possible value... by Weka," but per Holte, OneR "treats missing as a legitimate value" (a distinct category, not an imputation); per Witten & Frank, J48 does fractional-instance-weighting (splits an instance with a missing value into weighted pieces sent down every branch); and per Soman & Bobbie's own text, Naive Bayes "simply skips" unknown attributes. These are three different mechanisms, not one "Weka" behavior — the paper's description is imprecise about which algorithm does what. CDS's approach (skip NaN at the point of use, no imputation) is simpler and uniform across the whole pipeline, and doesn't inherit whatever bias each of these three distinct WEKA mechanisms introduces.
2. J48's rule-simplicity/interpretability advantage is real and CDS doesn't have an equivalent. Witten & Frank's pruned-rule extraction (§ "From trees to rules") produces a small set of human-readable IF-THEN rules with an explicit pessimistic-error-based simplification pass. Holte's whole "simplicity first" methodology argument is that a static-complexity/accuracy tradeoff exists and should be made visible, and OneR sits at the simple end of that tradeoff deliberately, as a benchmark. CDS has no equivalent artifact — its evidence-accumulator scores (af_for, af_against, Fisher-weighted feature contributions) are not something a clinician could read as a short rule list the way a pruned J48 tree or even a OneR 1-rule can be.
3. Naive Bayes's actual formal guarantee (Langley et al.) is a genuine strength the WEKA baselines have that CDS doesn't. Langley, Iba, and Thompson derive closed-form expected accuracy as a function of n, attribute count, and noise — an actual average-case theoretical guarantee, checked against experiment. There is no analogous formal characterization of CDS's behavior; its per-class threshold/support constants are empirically tuned, not derived. If Naive Bayes's assumptions held reasonably well here (a big if, given non-independence between many ECG features), its accuracy would be predictable in a way CDS's isn't.
4. 10-fold CV as the primary metric here is a step better than the Sharma paper's raw splits, but the split results reported alongside it undercut the paper's own reliability. Table 1 reports training-set-itself, 50/50, 70/30, and 80/20 in addition to what's presumably the 10-fold number — and the spread across these for the same algorithm (J48: 91.81 → 67.03, a 25-point swing) is larger than anything we saw in the Sharma paper's DS1–DS3. That's useful independent confirmation of the split-variance point from earlier, now visible in three algorithms' worth of data rather than one, and it strengthens the case that CDS's own eventual LOOCV numbers need to be read the same way — as the only member of that family of results actually resistant to this instability, not automatically superior to it.
The throughline is consistent with the earlier comparison: CDS's per-class accommodations are targeted, principled responses to specific mechanisms in OneR (single-attribute ceiling), J48 (majority-dominated splitting + pruning-away of small branches), and Naive Bayes (per-class parameter estimation with too little data) that are visible directly in these three source papers — while what CDS gives up is the maturity, interpretability, and (in Naive Bayes's case) formal theoretical grounding that these established methods bring.

# Paper 3: MODULAR NEURAL NETWORK BASED ARRHYTHMIA CLASSIFICATION SYSTEM USING ECG SIGNAL DATA

Where CDS is stronger
1. This paper's evaluation methodology has a genuine leakage problem, worse than anything seen so far. "DSMains" trains on all 452 instances and reports the same numbers as if they were performance results — 96.32%, 96.94%, 93.95% (Table 3) — but this is resubstitution error: training and "testing" on identical data. That's not a held-out estimate at all; it's an upper bound on how well the network memorized its own training set, and the paper reports it in the same table, at the same weight, as genuine train/test splits (DS1–DS5). Notice the pattern: DSMains's 93–97% dwarfs every genuine split's result (72–82%), which is exactly the gap you'd expect from overfitting rather than a genuinely better data configuration. CDS's fold-isolated retraining, whatever its other issues, never conflates "seen this data during training" with "evaluated on this data."
2. The missing-value imputation method may leak class-label information into the "unseen" data. "Missing attribute values... are replaced by closest column value of the concern class" — worded as: fill in a missing value using other instances of the same class. If this imputation is done before the train/test split (the paper never specifies otherwise, and it's described in §3.1 as a single blanket preprocessing step applied to "the entire database" before Data Set Groups are formed in §3.2), then a test instance's missing feature can be filled in using information derived partly from its own true label — which the model isn't supposed to have access to at evaluation time. This is a more serious version of the Sharma paper's pre-split feature-selection leak, because here it's literally using the target variable to construct an input feature. CDS never imputes at all — NaN is simply excluded from a feature's computation, with no dependency on any label.
3. No hyperparameter selection protocol, and results across depth are non-monotonic in an unexplained way. Sensitivity and specificity bounce inconsistently as hidden-layer count goes 1→2→3 within the same data set (e.g., DS1: sensitivity 73.68→65.79→71.05, specificity 82.69→84.62→84.62 — no clear trend), and the paper picks "2 hidden layers" as best largely by eyeballing Fig. 2/4 rather than any systematic search or held-out validation layer. Zhou's Ch. 5 treatment gives the likely explanation: backprop converges to different local minima depending on initialization, and with no mention of multiple restarts or seed control, this network's reported numbers per configuration are each a single draw from that variability — the same "single-run instability" problem flagged with CDS's own seed sensitivity, just undocumented here rather than measured. CDS at least has the virtue (however imperfectly exercised) of your own methodological rule requiring confirmation across many folds before trusting a result.
4. This paper also collapses to binary classification, same critique as Sharma: "these 15 classes are merged into a single class called 'Abnormal'" (§3.1). The 99.02% figure cited for Niwas et al. in the related-work section (§2) is for the genuinely harder 9-class task and is flagged by the authors themselves as "only for few classes" — an implicit admission that their own binary framing sidesteps the harder multi-class problem CDS is actually attempting.
Where the neural network approach could be stronger
1. Universal approximation is a real theoretical property CDS's tree-plus-Bayesian-evidence architecture doesn't have. Per Zhou's Ch. 5 (Cybenko, 1989), a three-layer MLP can approximate arbitrary decision regions arbitrarily well given enough hidden units — a formal expressiveness guarantee. CDS's evidence-accumulator design is a fixed functional form (weighted for/against ratios); there's no equivalent theorem saying it can represent an arbitrary decision boundary given enough features or nodes. If the true class boundaries in this dataset are highly nonlinear and don't decompose cleanly into per-feature bin-level evidence, an MLP has a structural capacity CDS lacks.
2. Regularization mechanisms are principled and battle-tested, versus CDS's ad hoc constants. Zollanvari's Ch. 13 covers dropout with a specific probabilistic justification (train-time random unit omission approximating an ensemble, with the (1-p) test-time rescaling to match expected activation) and early stopping tied to monitored validation metrics. This paper doesn't actually use either (no dropout or early-stopping callback is mentioned — training presumably runs to convergence or a fixed epoch count via NeuroSolutions defaults), which is itself a weakness relative to what the architecture could offer if used properly. But the underlying mechanism — a single global regularization knob rather than CDS's fragmented per-class threshold system — is at least a coherent, well-understood one when it is used.
3. Modularity is a real, if underexplained, structural idea that parallels CDS's OVR decomposition. The "Modular" in MNN suggests sub-networks handling different aspects of the classification, which is philosophically similar to CDS's per-class evidence accumulators — decompose a hard multi-way problem into more tractable sub-problems. Unfortunately the paper never actually describes how the modules are organized or combined (Fig. 1 just shows a generic multilayer diagram, no module boundaries drawn), so this is a strength the paper claims but doesn't substantiate — worth noting as a gap in the paper itself rather than a real point of comparison until better documented.
4. Backprop's gradient-based optimization scales to more features/parameters than CDS's greedy per-feature scoring. CDS scores each of 279 features independently per node per class (no interaction terms beyond what correlation-filtering removes); an MLP's hidden layer can in principle learn feature combinations jointly via gradient descent across the whole network simultaneously, rather than each feature being evaluated in isolation the way _train_ovr_node's scoring loop does. Whether that theoretical advantage materializes with only 452 patients and no dropout/regularization shown in this paper is a separate question — but the optimization framework itself is more expressive in principle.
Bottom line on this one specifically: the paper's biggest weaknesses (DSMains resubstitution reported as a result, likely label-leaking imputation, no seed/init control) are methodological rather than about the neural network architecture itself — so this comparison mostly reinforces the same lesson as the earlier two papers rather than adding a fundamentally new axis. The genuine architectural strengths of neural networks (universal approximation, joint feature-interaction learning via gradient descent) are real and worth taking seriously, but this particular paper doesn't demonstrate them convincingly given how it's evaluated.

Kian's Note: The "strengths" for the paper as outlined by Claude are guesses at best, considering the original paper does not mention how general aspects of neural networks, which was covered in typical machine learning textbooks, have benefitted the accurate diagnosis of arrhythmia is significant ways.

# Paper 4: Arrhythmia detection using machine learning: A study with UCI arrhythmia dataset

Where CDS is stronger
1. Same pre-split feature-selection leakage pattern as the Sharma paper, from the same lab. §4 describes correlation analysis ("the variables with the weakest correlations are excluded from the dataset") as a step performed on "the dataset" broadly, textually preceding the description of results per model — and nowhere does the chapter state the correlation filter was refit inside each train/test split. Given this is the same Pearson-correlation-based selection method as Sharma et al. (with an overlapping author), it's reasonable to suspect the same pre-split leakage mechanism is present here too: features get selected using information from what becomes the 10% test set. CDS's fold-isolated retraining doesn't have this exposure.
2. Real multi-class task collapsed to binary again, with a different split than the same institution's other paper. 198 arrhythmia vs. 253 normal here, versus Sharma et al.'s 207/245 — a ~10-instance discrepancy that's never explained, suggesting different missing-value handling between the two papers from the same group despite both using "the UCI Arrhythmia dataset." Regardless of the discrepancy, the underlying move is the same: 15 clinically distinct arrhythmia subtypes get folded into one "diseased" bucket before any model sees the data, so none of these four algorithms is actually attempting the task CDS attempts.
3. "Stratified cross-validation" is used ambiguously, and the reported results look like a single split, not aggregated folds. §3.3 says 90% train / 10% test; §3.4 says results are "validated" via "stratified cross-validation." But Table 2 and Figs. 4–7 each show one confusion matrix per model — there's no indication of results averaged across folds, no variance reported, no seed stated. This reads as a single stratified train_test_split call, with "cross-validation" used loosely rather than meaning k-fold CV was actually run and averaged. With ~45 patients in that 10% test set, this is the same small-hold-out instability problem flagged repeatedly across the other papers, compounded by unclear terminology that makes it hard to tell whether the reported 88% for SVM is even a single lucky draw.
4. The benchmarking table (Table 3) has the same apples-to-oranges problem as Sharma's Table 24. Comparing 88% (this chapter, single stratified split) against 86.6% (Voleti & Reddi, 10-fold CV), 84% (Anderies), and 85% (Grandhi & Singh, "stratified cross-validation") without controlling for evaluation protocol, class-imbalance handling, or dataset version repeats exactly the benchmarking flaw already discussed — small differences in accuracy across studies using different train/test protocols aren't meaningfully comparable, and CDS's eventual LOOCV number, once obtained, would be more directly comparable to none of these four than to each other.
Where the four combined ML models could be stronger than CDS
1. AdaBoost's boosting mechanism is a more principled ensemble strategy than CDS's ad hoc evidence weighting. AdaBoost reweights training examples adaptively based on prior misclassifications, with a formally derived exponential-loss-minimizing combination rule (Zhou's Ch. 8 material from earlier in this conversation covers the derivation). CDS's _compute_af also produces a weighted combination of per-feature "votes" (Fisher-weighted, confidence-scaled), but the weights are hand-set constants (support/confidence thresholds, against-scale) rather than something learned from training error the way AdaBoost's example-reweighting is. AdaBoost's 69% here is actually the weakest of the four in this chapter, but that's likely an artifact of running it on 279 raw/lightly-filtered features without the kind of per-class rare-class accommodation CDS has — the underlying boosting mechanism itself is a more rigorously justified way to combine weak signal than CDS's static weighting scheme.
2. SVM's margin-maximization gives a formal generalization guarantee that CDS's threshold-based decision rule doesn't have. SVM's 88%/100%/82.75% here is the best of the four, and structurally SVM is optimizing for the maximum-margin separating hyperplane — a criterion tied to VC-dimension-based generalization bounds (from the computational learning theory material covered earlier). CDS's predict() — comparing a for:against ratio against a set of hand-picked, per-class threshold constants — has no comparable theoretical guarantee that its decision boundary generalizes well; the thresholds were arrived at by iteration against reported accuracy, not derived from a margin or risk-bound argument.
3. KNN's instance-based approach makes no parametric assumptions and needs no per-node binning. CDS relies on binning each continuous feature into a handful of intervals (via _supervised_bin_edges) to compute class-conditional evidence — a discretization step that discards some information and introduces boundary-effect artifacts. KNN instead uses raw distance in feature space directly; for continuous physiological measurements like QRS duration or QT interval, that's arguably a more natural fit to the data's actual structure than CDS's chi-square-driven bin boundaries, which can be unstable with the same small-sample problem affecting rare classes (few examples means unstable optimal split points).
4. Decision Tree here at least attempts a genuinely data-driven multi-feature split structure, where CDS's tree is fixed and shallow. As established earlier, CDS's build_tree only ever splits on sex (feats = [SEX_FEAT] is hardcoded) and stops at depth 2. A standard decision tree, by contrast, searches all features at every node and can grow to whatever depth the data supports. Even acknowledging J48/CART's rare-class vulnerabilities discussed with the Soman & Bobbie paper, the DT here is at least structurally capable of discovering feature interactions CDS's fixed sex-only split can never find, since CDS's only structural (non-Bayesian-evidence) branching decision is hardcoded rather than learned from data.
5. All four benefit from being implemented in mature, widely-used libraries (the chapter names Anaconda/Python 3.8.5, implying scikit-learn), carrying the same "battle-tested implementation" advantage over CDS's bespoke code discussed with the Sharma comparison — worth restating here since it applies uniformly across all four models in this chapter too.
The pattern holds across every UCI-dataset paper we've now covered: the binary-collapse and single/small-split evaluation choices are the actual source of these papers' apparent performance advantage over CDS's harder, more rigorously-evaluated task — while the genuine architectural strengths (AdaBoost's adaptive reweighting, SVM's margin guarantee, KNN's non-parametric fit, unconstrained tree search) are real and worth CDS eventually being benchmarked against fairly, once both sides are run under the same protocol.

Kian's Note: Once again, some of the reasons stated above are not credible as: 1- They complain about how CDS uses multiclassification while other papers attempt at analyzing UCI through a binary perspective. 2- Complain about how hard it is to compare the results due to not specifying the type of validation method (usually cross-validation) used. 3- Additionally, one of the reasons goes as far as saying that since bining is slightly less stable regarding small samples, KNN's distance measure is a more natural fit for UCI.

# Paper 5: Multiclass Classification of Cardiac Arrhythmia Using Improved Feature Selection and SVM Invariants

Where CDS is stronger
1. Same pre-split feature-selection leakage as every other paper reviewed, now compounded by data duplication. §3.1's WFS procedure is explicitly run before the train/test split described in §3.3 ("the normalized and filtered dataset is partitioned into training and testing sets" comes after feature selection). Worse, the WFS step itself creates "at least five copies of each data instance... resulting in an increase in the number of records to a total of 2260" to train the shadow-attribute-comparison Random Forest — meaning near-duplicates of whatever ends up in the eventual test set are present during feature selection. This is a more aggressive version of the leakage already flagged in Sharma et al. and Kumar et al., since it's not just information leaking across the split, it's literal near-duplicate rows of test instances present during the selection step. CDS's fold-isolated retraining has neither issue.
2. Missing-value imputation (column mean) again appears to be computed on the full dataset before splitting, same pattern as Jadhav's closest-column-value approach and Devadas's MICE — another spot where the eventual test set's own values likely influence what gets imputed for training rows (and vice versa), since §3.1 describes it as a single preprocessing pass prior to the described §3.3 split. CDS never imputes.
3. Feature selection is one global set shared across all 16 classes, not class-aware — the same majority-class-domination problem flagged for J48's gain ratio, now applying to Random-Forest permutation importance. WFS scores features by average accuracy-loss-on-permutation across RF's decision trees — an importance measure necessarily dominated by whichever features help discriminate the numerically largest classes, since RF's bagged trees are fit on the overall class distribution. A feature uniquely diagnostic of a 2- or 3-instance class has little chance of showing up in the global top-94 rankings this way. CDS's _train_ovr_node scores every feature separately per class, so a feature can be selected for class 9 even if it's globally uninformative for the dataset as a whole.
4. The paper's own Table 1 is a clean, self-supplied demonstration of the split-size inflation problem this conversation keeps surfacing. OAO's accuracy climbs monotonically as test-set size shrinks: 73.45% (50/50) → 74.59% (60/40) → 77.20% (70/30) → 81.11% (80/20) → 92.07% (90/10). That's an 18.6-point spread purely as a function of how much data is held out, on the same algorithm, same features, same run. This is the single clearest illustration across everything you've uploaded of why a hold-out percentage isn't a property of the model.
Where this paper's SVM-decomposition approach is genuinely stronger (or at least more self-aware) than papers reviewed earlier — and than CDS in places
1. To their credit, the authors don't report the flattering number as their headline result. After showing the monotonic climb in Table 1, they explicitly write: "data overfitting occurs when large amount of data is used for training, so a standard 80/20 data split is used for further implementation" — and then use 81.11% (not 92.07%) for every subsequent comparison (Tables 2 and 3). That's a real methodological step up from Sharma et al., who reported their most-inflated 90/10 number (95.24%) as the headline result without this caveat. It doesn't fix the underlying pre-split leakage, but it shows awareness of one of the two problems (split-size sensitivity) that this conversation has been tracking.
2. OAA is structurally the same family of strategy as CDS's own OVR decomposition — a genuinely apples-to-apples architectural comparison. CDS's train() builds one evidence accumulator per class against the rest — that's OAA (one-against-all) by definition. This paper runs OAA directly and reports it losing to OAO (n(n-1)/2 pairwise binary classifiers, 92.07%/81.11%) at every split size in Table 1. That's a directly relevant empirical data point: on this dataset, decomposing into all-pairs binary problems outperformed one-vs-rest decomposition, for SVM specifically. Whether that generalizes to CDS's very different underlying binary-decision mechanism (margin-maximizing hyperplane vs. Bayesian-evidence-ratio) is an open question, but it's a legitimate reason to ask whether CDS's chosen decomposition (OVR) is actually the best one for this dataset's structure, rather than assuming OVR is neutral.
3. Feature-selection method comparison (Table 3) is a genuinely useful ablation CDS doesn't have an equivalent for. The paper directly compares its wrapper method (81.11%) against PCA (76.67%), correlation-based selection (60.00%), and information-gain selection (68.89%) — all under the same split, same classifier, same everything else. That's a controlled ablation isolating the effect of feature-selection method alone. CDS has never been run with an alternative feature-scoring approach substituted in for comparison; we don't actually know how much of CDS's behavior is attributable to its specific shift/confidence/Fisher scoring versus what a simpler or more sophisticated selector would produce holding everything else fixed.
4. Kappa statistic and RMSE alongside accuracy give a more complete picture than accuracy alone. Kappa corrects for the accuracy a random or majority-class predictor would achieve by chance, which is particularly relevant given the class-imbalance concerns raised throughout this conversation — a model could post a deceptively high raw accuracy on a skewed 16-class problem just by defaulting to Normal ECG often enough. CDS's stats() function currently reports accuracy/specificity/sensitivity/balanced-accuracy but not kappa; adding it would make CDS's eventual LOOCV results more directly comparable to this paper's Table 2/3, and would separately flag if CDS's HEALTHY-biased fallback design (discussed earlier) is inflating accuracy without a matching kappa improvement.
5. TSVM's poor showing (49.74%) is an implicit warning about tuning-dependence that applies to CDS's own hand-set constants. The authors note TSVM "does not give optimal results" despite trying a defined grid over c1, c2, kernel p, and tolerance — a sophisticated architecture underperforming simpler OAO/OAA/ECC because of parameter sensitivity on a small, imbalanced dataset. That's a cautionary data point for CDS too: CLASS_THRESHOLDS, MIN_SUPPORT_MAP, etc. are hand-set rather than grid-searched at all, so there's no equivalent evidence yet about how sensitive CDS's results are to those specific constant choices versus nearby alternatives.
Bottom line: this is the closest genuine architectural peer to CDS you've found — real 16-class task, an OVR-family baseline (OAA) run directly, and (uniquely among the papers reviewed) explicit acknowledgment of the split-size inflation problem. The leakage issues (pre-split WFS with duplicated rows, pre-split imputation) mean 81.11%/92.07% still can't be taken as clean ground truth, but the paper's OAA-vs-OAO result and its feature-selection ablation are both directly actionable comparisons once CDS has a fair (LOOCV, leakage-free) number to put next to them.

Kian's Note: This one didn't try that hard to provide reasons as to why the machine learning algorithm used was better, only saying that "it was more self-aware". The potential strengths it proposes compared to CDS are seemingly guessed. It does mention it as a worthy competitor though.

# Paper 6: Heartbeat Classification and Arrhythmia Detection Using a Multi-Model Deep-Learning Technique

Where CDS is stronger
1. Same pre-split feature-selection leakage as every other paper reviewed, now compounded by data duplication. §3.1's WFS procedure is explicitly run before the train/test split described in §3.3 ("the normalized and filtered dataset is partitioned into training and testing sets" comes after feature selection). Worse, the WFS step itself creates "at least five copies of each data instance... resulting in an increase in the number of records to a total of 2260" to train the shadow-attribute-comparison Random Forest — meaning near-duplicates of whatever ends up in the eventual test set are present during feature selection. This is a more aggressive version of the leakage already flagged in Sharma et al. and Kumar et al., since it's not just information leaking across the split, it's literal near-duplicate rows of test instances present during the selection step. CDS's fold-isolated retraining has neither issue.
2. Missing-value imputation (column mean) again appears to be computed on the full dataset before splitting, same pattern as Jadhav's closest-column-value approach and Devadas's MICE — another spot where the eventual test set's own values likely influence what gets imputed for training rows (and vice versa), since §3.1 describes it as a single preprocessing pass prior to the described §3.3 split. CDS never imputes.
3. Feature selection is one global set shared across all 16 classes, not class-aware — the same majority-class-domination problem flagged for J48's gain ratio, now applying to Random-Forest permutation importance. WFS scores features by average accuracy-loss-on-permutation across RF's decision trees — an importance measure necessarily dominated by whichever features help discriminate the numerically largest classes, since RF's bagged trees are fit on the overall class distribution. A feature uniquely diagnostic of a 2- or 3-instance class has little chance of showing up in the global top-94 rankings this way. CDS's _train_ovr_node scores every feature separately per class, so a feature can be selected for class 9 even if it's globally uninformative for the dataset as a whole.
4. The paper's own Table 1 is a clean, self-supplied demonstration of the split-size inflation problem this conversation keeps surfacing. OAO's accuracy climbs monotonically as test-set size shrinks: 73.45% (50/50) → 74.59% (60/40) → 77.20% (70/30) → 81.11% (80/20) → 92.07% (90/10). That's an 18.6-point spread purely as a function of how much data is held out, on the same algorithm, same features, same run. This is the single clearest illustration across everything you've uploaded of why a hold-out percentage isn't a property of the model.
Where this paper's SVM-decomposition approach is genuinely stronger (or at least more self-aware) than papers reviewed earlier — and than CDS in places
1. To their credit, the authors don't report the flattering number as their headline result. After showing the monotonic climb in Table 1, they explicitly write: "data overfitting occurs when large amount of data is used for training, so a standard 80/20 data split is used for further implementation" — and then use 81.11% (not 92.07%) for every subsequent comparison (Tables 2 and 3). That's a real methodological step up from Sharma et al., who reported their most-inflated 90/10 number (95.24%) as the headline result without this caveat. It doesn't fix the underlying pre-split leakage, but it shows awareness of one of the two problems (split-size sensitivity) that this conversation has been tracking.
2. OAA is structurally the same family of strategy as CDS's own OVR decomposition — a genuinely apples-to-apples architectural comparison. CDS's train() builds one evidence accumulator per class against the rest — that's OAA (one-against-all) by definition. This paper runs OAA directly and reports it losing to OAO (n(n-1)/2 pairwise binary classifiers, 92.07%/81.11%) at every split size in Table 1. That's a directly relevant empirical data point: on this dataset, decomposing into all-pairs binary problems outperformed one-vs-rest decomposition, for SVM specifically. Whether that generalizes to CDS's very different underlying binary-decision mechanism (margin-maximizing hyperplane vs. Bayesian-evidence-ratio) is an open question, but it's a legitimate reason to ask whether CDS's chosen decomposition (OVR) is actually the best one for this dataset's structure, rather than assuming OVR is neutral.
3. Feature-selection method comparison (Table 3) is a genuinely useful ablation CDS doesn't have an equivalent for. The paper directly compares its wrapper method (81.11%) against PCA (76.67%), correlation-based selection (60.00%), and information-gain selection (68.89%) — all under the same split, same classifier, same everything else. That's a controlled ablation isolating the effect of feature-selection method alone. CDS has never been run with an alternative feature-scoring approach substituted in for comparison; we don't actually know how much of CDS's behavior is attributable to its specific shift/confidence/Fisher scoring versus what a simpler or more sophisticated selector would produce holding everything else fixed.
4. Kappa statistic and RMSE alongside accuracy give a more complete picture than accuracy alone. Kappa corrects for the accuracy a random or majority-class predictor would achieve by chance, which is particularly relevant given the class-imbalance concerns raised throughout this conversation — a model could post a deceptively high raw accuracy on a skewed 16-class problem just by defaulting to Normal ECG often enough. CDS's stats() function currently reports accuracy/specificity/sensitivity/balanced-accuracy but not kappa; adding it would make CDS's eventual LOOCV results more directly comparable to this paper's Table 2/3, and would separately flag if CDS's HEALTHY-biased fallback design (discussed earlier) is inflating accuracy without a matching kappa improvement.
5. TSVM's poor showing (49.74%) is an implicit warning about tuning-dependence that applies to CDS's own hand-set constants. The authors note TSVM "does not give optimal results" despite trying a defined grid over c1, c2, kernel p, and tolerance — a sophisticated architecture underperforming simpler OAO/OAA/ECC because of parameter sensitivity on a small, imbalanced dataset. That's a cautionary data point for CDS too: CLASS_THRESHOLDS, MIN_SUPPORT_MAP, etc. are hand-set rather than grid-searched at all, so there's no equivalent evidence yet about how sensitive CDS's results are to those specific constant choices versus nearby alternatives.
Bottom line: this is the closest genuine architectural peer to CDS you've found — real 16-class task, an OVR-family baseline (OAA) run directly, and (uniquely among the papers reviewed) explicit acknowledgment of the split-size inflation problem. The leakage issues (pre-split WFS with duplicated rows, pre-split imputation) mean 81.11%/92.07% still can't be taken as clean ground truth, but the paper's OAA-vs-OAO result and its feature-selection ablation are both directly actionable comparisons once CDS has a fair (LOOCV, leakage-free) number to put next to them.

EXTRA SUPPORTED BY PAPER RATHER THAN JUST CODE: CDS's MIN_SUPPORT_MAP/RARE_CLASSES design is a direct, principled response to exactly the n=1-training-instance scenario this paper's V and LV classes represent. CDS doesn't pretend a class with 1–4 training instances can support the same statistical confidence as a class with 245 — it explicitly lowers the support/confidence bar and widens tolerance for those classes rather than reporting (and implicitly trusting) a 100% result built on a single training example. Table 10's V/LV/S/A rows are what happens when an architecture has no such accommodation: the numbers look perfect precisely because there was never enough data for the claim to be falsifiable in the first place, compounded by the leak.
This also sets a concrete bar for what CDS's own eventual LOOCV rare-class numbers need to look like to be trustworthy. Under true LOOCV, a class with 2–4 total instances will still only ever contribute 2–4 individual pass/fail data points to its balanced-accuracy estimate — structurally similar to this paper's tiny test counts. The difference is that LOOCV (unlike this paper's single 60/40 split) evaluates every instance of every rare class exactly once as a genuinely held-out case, with no PCA/scaler fit crossing the boundary — so if CDS's rare classes come back at 100% under LOOCV, that result would actually mean something, whereas this paper's superficially identical 100% rows do not.

Kian's Note: Overall, this comparison wasreasonable considering the code was already provided before.

Kian's Note: The reasons stated as to why CDS is better is not strong as Claude only cares about how the results are not reproducible and thus, cannot be trusted, alongside how the results were named in a paculiar way.